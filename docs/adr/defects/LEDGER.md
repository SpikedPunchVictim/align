# Defect ledger

Newest first. One row per defect. See `README.md` for why the "should have caught it" column is the
one that matters, and `SHAPES.md` for the derived catalogue that feeds review briefs.

**Severity**: `S0` = destroys data and reports success (CLAUDE.md rule 6). `S1` = wrong result a user
would act on. `S2` = misleading output, no data loss. `S3` = internal/doc inaccuracy.

**Confidence**: `measured` = reproduced during this project's own work, numbers taken from a run.
`reconstructed` = backfilled from doc comments and ADRs after the fact; the shape is reliable, the
incidental detail is not.

| ID | Date | Sev | Discovery instrument | What was wrong | Shape | Which check should have caught it | Fix + pin | Conf |
|---|---|---|---|---|---|---|---|---|
| D071 | 2026-08-21 | S3 | Verifying a claim I had just made — grepping the release matrix's own normalized artifacts to check that the newly-derived version list actually scrubbed `0.2.1`, rather than asserting it did | **A version string at the END OF A SENTENCE is never normalized.** `KNOWN_VERSION_PATTERN` (`integration/lib/normalize.mjs`) guards its match with `(?![\d.])`, added by F7 so that `0.1.4` cannot match inside `10.1.4`. That lookahead also rejects an ordinary full stop, so `reconciled under align 0.2.1.` keeps its raw version while `reconciled under align 0.2.1` is normalized. Reproduced directly: the first string passes through `normalizeText` unchanged, the second becomes `<normalized-version>`. Real instance in the 2026-08-21 release matrix — `partial-checkout-retains` step 10 normalized **all 60-odd** version strings in `align upgrade`'s output and left exactly one, the trailing `Reconciled — baseline is now recorded as reconciled under align 0.2.1.` Affects every version equally, including `0.1.4`, and predates the 0.2.0 -> 0.2.1 bump | [S-04] — a guard correct in the unsafe direction (never normalizes a third-party version) and wrong in the safe one (fails to normalize a genuine one). Same shape as the F7 defect it was written to fix, one boundary over | **Nothing, and the F7 fix is why it looked covered.** F7 added the boundary and a comment explaining the `10.1.4` case in detail, which reads as the boundary having been thought through. The test that pinned F7 asserts the third-party case survives; no test asserts a sentence-final version is scrubbed, so the half that regressed had no witness. `align-version.test.ts`'s new end-to-end case did not catch it either — the sentence it normalizes happens not to end in a version | **OPEN — not fixed, deliberately, and it is not a release blocker.** The matrix passed and no scenario asserts on an affected line, so nothing is reported wrongly today; the cost is that one captured line per `upgrade` run is not version-independent. The fix is small — replace `(?![\d.])` with a lookahead that rejects only a following DIGIT or dot-then-digit, e.g. `(?!\.?\d)`, so `0.1.4.5` is still refused and `0.2.1.` is scrubbed — but changing normalization shifts every captured artifact and therefore needs its own matrix run to re-verify. Doing that mid-release would invalidate the gate that just passed. Pin it with both directions when fixed: sentence-final version scrubbed AND `10.1.4`/`@nestjs/core@10.1.3` still surviving | measured |
| D070 | 2026-08-21 | S2 | Probing the D063 fix outward — asking which OTHER rule families identify a finding by a tuple that omits something the graph knows | **`arch.*` gives a type-only import and a value import of the same specifier one identity, which is D063's shape in a second rule family.** Re-derived this session rather than recalled: `computeFingerprint(['no-dependency', rule.id, edge.from, target.file, edge.specifier])` (`evaluators.ts:149`) and the four sibling arms at lines 193, 262, 309 and 356 take no `edge.kind`, while `DependencyGraph`'s `Edge` carries one as a first-class field — *"type-only is first-class, 32% of edges measured (ADR 004)"* (`types/graph.ts:25`). A file doing `import type { A } from './b'` **and** `import { c } from './b'` produces two edges that differ in `kind`, `line` and `snippet`, both violate the rule, and both hash the same. Accepting one accepts the other, including one added later. By D063's own `sameObservableFinding` test (same range, same snippet) these are NOT the same finding, so the mechanism align just built to refuse this says these two should be refused | [S-14] — an identity narrowed (or never widened) without asking what it was keeping apart, the same shape as D063. Second instance of the family, which per this repo's own rule is what earns a shape promotion | **Nothing, and D063's fix is deliberately scoped away from it**: `refuseOnCollision` lives in `host-rules.ts` and guards `custom.host` only. Nothing enumerates the fingerprint tuples across rule families and asks which graph facts each one drops — the check that would have found this and D063 together, and would answer the completeness question rather than the instance question | **OPEN — no fix, deliberately out of 0.2.1**, and the reason is the fix's blast radius rather than doubt about the finding. Adding `edge.kind` to the tuple re-identifies **every accepted `arch.*` baseline entry in every repository using align**, and move-transfer (ADR 006) will not rescue them: transfer matches on content fingerprint for a file that MOVED, and here the file has not moved, so the old entries orphan and the new ones arrive unaccepted. That is a migration with a `UPGRADING.md` note and a version bump behind it, not a patch-release change. Recorded now, unfixed, because a row with no fix is useful and a fix with no row loses how it was found. **Not yet measured**: how many real entries a `kind`-aware identity would actually split — the n8n and nest baselines can answer that and were not run | reconstructed |
| D069 | 2026-08-20 | S2 | Adversarial review of `integration/` by a subagent, then confirmed by enumerating the key sets in `lib/spec-validate.mjs` against the ones scenarios actually use | **The file whose entire purpose is rejecting a silently-ignored key never checked the outermost level.** `validateScenario` enumerates a known-key set for `expect`, for `assert` (per `kind`), for `mcpCall`, for its spec, and for each step kind — and accepts any top-level key on the scenario object itself. So `expectFailsOn: ['0.1.4']` loaded clean and **silently un-pinned a calibration scenario**: it drops out of the ten that constitute the harness's proof it can still detect a regression, and nothing says the count moved. `tag:` for `tags:` drops a scenario out of every `--tags` selection forever. `writeset:` degrades to ADR 026's fail-closed empty default, which fails in the safe direction but names the wrong cause. Two adjacent gaps found the same way: nothing rejected a duplicate scenario `id` (three consumers resolve an id to the first match, including `calibrationBreaks`'s `matrix.find`), and nothing rejected `expectFailOn: ['local']`, which asks the gate target to fail and would read a genuine gate failure as the expected result | [S-05] — a scenario that keeps passing while asserting less than it claims, which is this file's stated reason to exist, at the one level it did not cover. Also [S-09]: five key sets enumerated, the sixth missed | **`spec-validate.mjs` itself, and it is the only candidate.** Every nested vocabulary was enumerated at the moment its level was written; the scenario object's own vocabulary was never written as a level, so no one enumerated it. `validateNoDuplicateKeys` (S-05's earlier promotion) reads the source for duplicate keys and is blind to unknown ones | `KNOWN_SCENARIO_KEYS` = `id, project, description, steps, writeSet, tags, expectFailOn`; anything else is a load-time throw naming every unknown key at once. `expectFailOn` additionally refuses `'local'` and duplicate targets; `description` must be a non-empty string when present. `validateNoDuplicateIds` runs over the whole corpus in `loadScenarios`, where `validateScenario` (one scenario at a time) structurally cannot see a collision. Verified against the real corpus: all 28 scenarios pass the stricter validator unchanged, and the pin count it reports is **10**, matching CLAUDE.md's grep-derived figure from an independent method. Pinned by `lib/spec-validate.test.mjs` (10) | measured |
| D068 | 2026-08-20 | S0 | Adversarial review of `integration/` by a subagent (`run.mjs:228`), confirmed by reading the same file's working-copy-retention arm, which already draws the distinction | **A pinned scenario that ERRORED counted as calibration success, and the run said so out loud and exited 0.** `expectFailOn` claims a scenario goes red against a published version *by failing its assertions*, because that version demonstrably has the defect. The check asked only `if (m !== undefined && m.pass)`, so a pinned pair that blew up before reaching its assertions — a failed install, a Docker hiccup, a harness bug — satisfied its pin. The run then printed *"all N pinned scenario(s) went RED as required"*, which is the release gate's whole calibration claim and the sentence CLAUDE.md instructs a human to read, over evidence it had never collected. The ten pinned scenarios are the only evidence this harness can still detect a real regression; **an environment that broke all ten would have reported perfect calibration** | [S-09] — the same file computes `!result.pass && !result.errored && pinnedToFailHere` one arm over, for working-copy retention, with a comment stating that a pin covers assertion failure and "never" the harness blowing up. One arm fixed, the other missed. Also [S-13]: the failure disables the instrument that would report it, and [S-10] — an unrun assertion read as a satisfied one | **Nothing could have. `calibrationBreaksIn` was a closure over `scenarios`/`matrix` inside `run.mjs`'s `main`** — the one function the release gate's calibration claim rests on was the one function no test could call, and `integration/` had no test runner at all. That is the finding behind the finding: the harness that verifies align was itself unverified | Extracted to `lib/calibration.mjs` as a pure function and fixed: an ERRORED pinned pair is a break, reported as `ERRORED` rather than conflated with `PASSED` — opposite diagnoses (the bug is gone / nothing was learned) that a reader deciding whether to ship needs told apart. Measured red-before-green in that order: 7 tests, 2 failing on the old logic, 7 passing after. **The durable half is the test runner**, since the fix is three lines and the reason it survived was structural: `pnpm test:harness` (`node --test`, no new dependency) runs `integration/lib/*.test.mjs`, gated ahead of every project in `scripts/integration-all-projects.mjs` — so `integration:dev`, `integration:release` and CI all refuse to report a scenario result over an unverified harness. Pinned by `lib/calibration.test.mjs` (7). **No past run was actually mis-reported, and this is measured rather than assumed**: all 26 archived runs in `integration/results/` whose targets include `0.1.4` were audited programmatically for a pinned row with `errored:true`, and there are none. The one errored row in the last full matrix (`2026-08-20T07-49-40-228Z`) is `scan-history-record-written`, which is deliberately UNPINNED and says so in its own header. So the defect was latent the whole time — which is the reason it survived review: the instrument had never yet been asked the question it would have answered wrongly | measured |
| D067 | 2026-08-20 | S1 | Structured repro bundle from the reporter (fixture 08), reproduced on the built binary | **The exported IR drifts from `align.config.ts` and no surface says so.** One tree, one moment: `align check` **green**, `align check --untrusted` **RED**, and neither command — nor `doctor` — mentioned that the two were reading different rules. `--untrusted` (ADR 014) reads `.align/ruleset-ir.json` instead of executing the config, and the documented workflow (*export when trusted, `--untrusted` in CI*) makes drift routine rather than exceptional. It fails in both directions: a stale IR keeps enforcing a rule the config dropped, and **misses one it gained — a false green in CI**. **A real instance was sitting in align's own repository and the new guard found it within a minute of being wired up**: the committed `.align/ruleset-ir.json` lacked `docs/adr/proposals` and `.claude` in `excludes`, so `align check --untrusted` on align itself was scanning two directories the trusted check does not | [S-10] — the silence of both surfaces was the only evidence anyone had that they agreed, and it was not evidence of anything | **Nothing, and there was no obvious place for it.** No test compares the two artifacts, and neither command has a reason to look at the other's input: the trusted path never reads `ruleset-ir.json`, and the untrusted path is DEFINED by never reading the config. The gap is exactly at the seam the flag creates, which is why an ADR-level feature could ship complete and still leave it | Two guards, because the modes can answer different questions and conflating them would produce a guarantee align cannot keep. **Trusted** (`check`, `doctor`) loads both and compares them EXACTLY — ruleset hash plus `excludes` plus `includeNestedCheckouts`, since all three are exported precisely because they change what `--untrusted` does — and names which part differs. **Untrusted** may not execute the config, so `export-ir` now stamps `sourceFingerprint`, a hash over `align.config.ts` + `.align/generated-rules.json`, and the check recomputes it from disk. That is strictly weaker and the advisory says only what it can support: a config that IMPORTS another module can change without moving the fingerprint, so a mismatch proves drift and a match proves nothing. An IR with no fingerprint (every 0.2.0-and-earlier artifact) reports that align **CANNOT tell** rather than staying quiet, because quiet is indistinguishable from verified-fresh [S-10]. Advisory, never a verdict flip: the trusted run read the live config and its verdict is right — what is wrong is a run somewhere else, and reddening the correct run punishes the person not making the mistake. Verified on the reporter's fixture and by round-trip (fresh export → both quiet; comment-only edit → untrusted warns, trusted stays quiet, which is the asymmetry working). Pinned by `stale-exported-ir.test.ts` (7), including all three quiet cases, and at the integration level by `stale-exported-ir-is-reported` (2026-08-21), which asserts BOTH halves of the asymmetry in one run against nest: a comment-only config edit warns on the untrusted path and stays silent on the trusted one. Asserting only the warning would pass an align that warned unconditionally; asserting only the silence would pass an align that never warned. Its drift case uses `excludes` — the clause that caught the real instance — with a pattern matching nothing, so `excludes` differs while the scan result is byte-identical and the advisory cannot be an artifact of a changed verdict | measured |
| D066 | 2026-08-20 | S2 | The reporter's amendment to finding 1 — reading the uncertainty vocabulary against the code that fills it | **The detector for D052 already existed, was already correctly named, and fired on a different question.** `UncertaintyReason` has enumerated `build-output-excluded` since ADR 004; the scanner raised it from `isExcludedPath(targetRel, excludes)` — the USER's config patterns. So an import into `dist/`, the literal build output the reason is NAMED for, produced **no marker at all** (fixture 01 emitted nothing, which is a large part of why D052 was silent), while an import into a user-excluded fixture directory WAS reported as build output, a claim the scanner has no way to support. `fixture-excluded` sat in the same enum produced by nothing — one definition, one doc mention, zero call sites | [S-05] pointed at a diagnostic rather than a test: the label and the predicate under it disagreed, and being green about the wrong question looks identical to being green. The dead enum member is [D051]'s shape — a seam wired to nothing | **Nothing, and the enum is why it looked covered.** A reviewer asking *"does align notice imports into build output?"* finds a reason named for exactly that and stops. The vocabulary was richer than the code, which reads as coverage | Both halves now report what the WALK RECORDED rather than a guess about why: `UncertaintyMarker` gains `excludedBy?: ScanBlindSpotReason`, reusing ADR 028's already-discriminated record instead of a second vocabulary, so the per-edge marker and the per-subtree blind spot cannot describe one event two ways. `build-output-excluded` is narrowed to targets under a default-excluded BUILD directory; everything else becomes `excluded-from-scan`, which claims only what it knows and replaces `fixture-excluded`. The default-excluded arm is a POST-PASS over `edges` against `blindSpots`, not an inline test, because an inline test cannot be correct: whether `build/` is output depends on whether its parent holds a `package.json` (D053), and the conservative predicate would invent markers for real source directories named `build`. Edges are KEPT, not dropped — they are already inert in every evaluator and `custom.host` predicates can see them, so removing them would change what a predicate observes for a diagnostic's benefit. **Found a true instance on align's own repository on its first run**: `packages/cli/scripts/compile-upgrading-notes.mjs` imports `../dist/migrations/notes-compiler.js`, now reported with `excludedBy {default-excluded-dir, dist}`. Pinned by `excluded-target-markers.test.ts` (4), including an invariant that every out-of-scope marker names the blind spot responsible | measured |
| D065 | 2026-08-20 | S2 | Probing the D061 fix — asking what `arch.layers` does with the unmapped files `cannotDependOn` had just been taught about | **`plugin-typescript`'s internal sentinel was printed to users as if it were one of their own layers.** `arch.layer(a).canOnlyDependOn(b)` against a target in no component rendered *"imports src/shared/r.ts (layer '\_\_unmapped\_\_')"*. The user never wrote `\_\_unmapped\_\_`, it is not in their config, and grepping for it finds nothing — so a real finding reads as align malfunctioning. **"Cosmetic" was the wrong guess, and measuring it is what showed that.** `canOnlyDependOn` is an allowlist, so an unmapped target is outside it BY CONSTRUCTION. On n8n with `cli canOnlyDependOn core`: **7577 violations, 7577 of them naming the sentinel** — not an edge case in the output, the whole of it | [S-15] (new — an internal sentinel presented as the user's own vocabulary) | **Nothing. The sentinel is exported deliberately** (`UNMAPPED_COMPONENT`, so `doctor`'s unmapped-files advisory need not duplicate the literal), which makes it look like a considered part of the contract. It is fine in `doctor`, where the surrounding sentence explains it, and wrong in a violation message, where it occupies the slot a user's own component name occupies. Nothing distinguished the two uses | The violation carries `toIsUnmapped` — decided the way D061 decides it, *a component the ruleset never declared*, so core still needs no knowledge of the plugin's string — and the message says **"(which matches no component selector)"**. `toLayer` is left carrying the plugin's classification, which is a faithful report of the scan; what changes is what align SAYS. **No fingerprint moves**: the change is purely additive on the violation object and `git diff` on `evaluators.ts` touches no `computeFingerprint` call, so no accepted baseline entry is re-identified. (Stated from the diff rather than from a baseline run — align's own baseline holds 29 entries and every one is `security.manifest.new-dependency`, so the dogfood could not have exercised the layers id either way.) Measured after, same repository: 0 mention the sentinel, 7577 use the new wording, count unchanged. **A counting correction worth keeping**: a bare `grep -c` answered 6956 for the new phrase, because it wraps across two lines for 621 of them. Lines are not violations; the real figure came from splitting on the `✗` marker and normalizing whitespace inside each block. Pinned by `unmapped-in-messages.test.ts` (3), the third of which is the executable invariant — **no evaluator may render a component the ruleset never declared** — driven over every rule kind rather than over the one arm that leaked | measured |
| D064 | 2026-08-20 | S2 | Structured repro bundle from the reporter (finding 9) | **An argument the user typed was accepted, discarded, and never mentioned.** `align build ARCHITECTURE.md` parsed the positional, dropped it, and ran against `docs/ARCHITECTURE-RULES.md`. In the reporter's repository that default does not exist, so they got *"Doc not found: docs/ARCHITECTURE-RULES.md"* — an error naming a path they never typed, which reads as align failing to find THEIR file. Measured in align's own repository, where the default DOES exist, it is worse: **exit 0 and a complete build report for a document nobody asked about**, which is this project's severity-zero shape in miniature | [S-09] — the report named one command and commander 12 defaults `allowExcessArguments` to TRUE, so every one of the ~20 commands had the same arm. Fixing only `build` would have been the textbook instance-not-class fix | **Nothing reads a command's argument contract**, and the permissive behaviour is a library default rather than anything written in this repository — there is no line to review. `align skill`'s CLI inventory walks the live command tree but only to describe it | Two layers. The durable one: `allowExcessArguments(false)` applied by WALKING `program.commands` recursively at the end of `buildProgram`, so a command added tomorrow is covered the day it is added — a hand-written list here would reproduce the defect one level up, which is the mistake `integration-all-projects.mjs` exists to stop making. The specific one: `build` declares `[doc]`, because the obvious reading should be the real one, and refuses when both it and `--doc` are given rather than silently ranking them. Measured after: `build NOPE.md` exit 1 naming NOPE.md; `build A.md --doc B.md` exit 2; `doctor oops` exit 2; declared positionals and subcommand dispatch unaffected. **The first version of the conflict refusal was wrong in a way its own test could not see** — it threw a `CommanderError` with code `align.build.conflictingDoc`, and `commanderExitCode` only maps codes prefixed `commander.`, so the real binary printed a raw stack trace while `rejects.toThrow(/both/i)` passed. The test now asserts the EXIT CODE: an assertion that stops at *it threw* cannot tell a handled usage error from an unhandled crash [S-05]. Pinned by `no-silently-discarded-arguments.test.ts` (5) and, at the integration level, by `build-positional-doc-is-not-discarded` (2026-08-21), which carries `expectFailOn: ['0.1.4']` — **calibrated, not assumed**: against the real published 0.1.4 the harness reproduces the reporter's exact symptom, `build ARCHITECTURE.md` printing `Doc not found: docs/ARCHITECTURE-RULES.md`, plus `doctor oops` exiting **0**. The doc is written to the repo ROOT precisely so that reading the argument and ignoring it produce different output — at the default path the two are indistinguishable and the scenario would pass against the defect [S-05] | measured |
| D063 | 2026-08-20 | **S0** | Structured repro bundle from the reporter (fixture 09), run against the published 0.2.0 binary | **One accepted baseline entry suppresses every `custom.host` finding that shares its file and message, including findings added after the acceptance, and the run exits 0.** Measured on `@spikedpunch/align-cli@0.2.0`: a predicate returning three findings for `src/index.ts` all messaged `bad thing here` (lines 1, 2, 3) against a baseline holding ONE entry gives `verdict: green`, `baselinedCount: 3`, `baselineDebt {previous: 1, current: 1, delta: 0}`, exit 0. **The debt line is the aggravating part** — it counts ENTRIES, so the 3-suppressed-by-1 collapse is invisible to the one instrument a human would audit, which reports that nothing changed. **Introduced by a fix**: `9102847` (2026-08-06) dropped `String(range.startLine)` from the id to stop baseline churn, correctly. The line number had also been the only thing separating two findings. `git tag --contains 9102847` = `v0.2.0` alone; `v0.1.4` still folds the line in, so 0.1.x churns and does not collapse. **This defect is 0.2.0-only** — unlike D052, which every published version has — though downgrading trades it for the churn 0.2.0 fixed and for D052/D053/D061 | [S-14] (new — an identity narrowed to stop churn, without asking what it was keeping apart), with [S-03] for why it survived | **An existing test, which asserted the defect was correct.** `host-rules.test.ts`'s *"collapses two same-file, same-message findings on different lines into one fingerprint — **intended, not accidental**"* pinned exactly this behaviour, reasoning that it is *"the same collision behavior every other rule family already has"*. The premise is half true and the conclusion inverts: the edge rules collapse two reports of ONE fact (the same import seen twice); this collapses findings a predicate deliberately raised as different. So the honest answer to this column is worse than "nothing" — a test can pin the wrong behaviour as firmly as the right one. The obligation it deferred to (`HostViolation`'s doc comment telling authors to put the distinguishing detail in `message`) was never checked anywhere, which is CLAUDE.md rule 5 in its user-facing form | `evaluateCustomHost` REFUSES: two normalized violations sharing an id whose `range` or `snippet` differ raise `HostViolationCollisionError` (gate `error`, exit 1, via the orchestrator's existing predicate-failure path), naming the rule, predicate, file, colliding message, count, and the one-line remedy. **Refusal, not disambiguation, because the predicate is the only party that knows** whether two findings are one problem — core has structural coordinates for every other rule kind and none for this one. Positional numbering re-points accepted entries when an earlier finding is fixed (consent forging, the thing ADR 027/028 exists to stop); falling back to the line restores the churn `9102847` removed and makes acceptance depend on iteration order; collapsing to one finding is the false green renamed. The identical-finding case (every observable field equal, e.g. two overlapping detection passes) still collapses to one violation, so the error only fires when there really are two. End-to-end on the fixture: green/exit 0 → error/exit 1. On the REPAIRED form: two distinct messages accepted → green; a third added → **red and named**; and reporting the same two findings ten lines lower stays green, so line-shift stability — the whole point of `9102847` — is preserved. Pinned by `host-violation-collision.test.ts` (8), and at the integration level by `host-rule-collision-refused` (2026-08-21) — a real `hostRules` export in a real `align.config.ts`, driven through the real binary against nest, proving the refusal AND that it LIFTS once the messages differ (a scenario that only showed align refusing would be satisfied by an align that refused every host rule). Deliberately UNPINNED: published 0.1.4's fingerprint is `['custom', rule.id, hv.file, String(range.startLine), hv.message]` — verified two ways, neither of them recall: by reading the unpacked 0.1.4 tarball, and by running the scenario against a real 0.1.4 through the harness, which reported `architecture RED 2 violation(s)` with both findings listed separately. 0.1.4 does not collapse them, so it does not have this defect and `expectFailOn: ['0.1.4']` would be a false claim plus the inverted existing test — 25 tests across both files. Guard mutation-checked four ways against the FINAL code: never refuse → 7 fail; treat every colliding pair as identical → 5 fail; drop `snippet` from the comparison → 1 fail; **keep only colliding groups, discarding every unique finding → SURVIVED**. That survivor is why there is an eighth test: every earlier case had either no duplicates or nothing BUT duplicates, so a mutation that silently deleted real violations from the report passed the entire file. A fifth mutation (keep the last member of a duplicate group instead of the first) also survives and is left unpinned deliberately — within a group every member is `sameObservableFinding`, so it is an equivalent mutant, and a test for it would pin an arbitrary choice | measured |
| D061 | 2026-08-20 | **S0** | Field report from a user authoring a real ruleset, reproduced immediately on the current build | **A file belonging to no component launders a forbidden edge, and the verdict stays green.** With `a cannotDependOn b`: `import from '../b'` is correctly RED, and `import from '../shared/relay'` — where `src/shared/relay.ts` is one line, `export * from '../b'`, in a directory matching no selector — is **green**. `align check` says nothing; `doctor` lists the relay among "N file(s) matched no component selector", giving it the same weight as `align.config.ts`. Files matching no selector are still graph NODES carrying a sentinel component, and every internal rule arm matches `fromNode.component === rule.from && toNode.component === rule.to`, so `a -> relay` and `relay -> b` each fail the test and the pair is invisible. **Severity is about reachability**: one line, no tooling, trivially discoverable by anyone working AROUND a rule, and reachable by accident — 523 unmapped files in the reporting repository, 135 measured in n8n | [S-09] | **Nothing, and `arch.layers` is why it looked covered.** `canOnlyDependOn` is an allowlist, so an edge to an unmapped file is already outside it and already a violation — layers was never laundered. Only `cannotDependOn` is target-specific, so only it can miss a target it was never pointed at. A reviewer checking "does align handle unmapped files" finds layers handling them correctly and stops | Unmapped nodes are CONTRACTED: an edge whose target belongs to no DECLARED component resolves to whatever mapped files it reaches through further unmapped nodes. **Not "rules are transitive"** — a MAPPED intermediary still absorbs the dependency, because a component is an architectural entity that owns it, and making rules transitive would light up every repository. An unmapped file owns nothing; it is a hole in the component map. "Unmapped" is decided the way `ungoverned-edges.ts` already decides it — a component the ruleset never declared — so core still needs no notion of the plugin's sentinel. The violation carries `relayedThrough` and the message names the chain, because a report that cannot name the relay tells the user `a` imports `b` via a specifier mentioning neither. Fingerprints are computed over the RESOLVED target, so a direct edge's id is unchanged — measured identical (`fb779ee46113af78`) before and after. **Measured at scale: n8n's violation count unchanged at 205** with 135 unmapped files, so the contraction does not explode real repositories. Pinned by `unmapped-relay.test.ts` (7): relay chains, an unmapped CYCLE terminating, a mapped intermediary still exempt, and the layers case pinned as NOT-vulnerable so nobody "fixes" it to match [S-05]. Contraction mutation-checked. **Eleven test fixtures passed `{}` as the components map** — an input `validateRuleComponentRefs` forbids in production — and were made faithful rather than worked around | measured |
| D062 | 2026-08-20 | S2 | **Pointing align at a real 300-package pnpm monorepo (n8n) as the first act of the corpus-shape work** — the discovery run existed to find issues before freezing 0.2.1's scope, and the first thing it found was a defect in a fix committed two hours earlier | **D055's exports-map filter was INERT under pnpm, the layout align targets most.** `doctor` called `packageDeclaresExportsMap(rootDir, pkg)`, and the lookup walks `node_modules` UPWARD from the directory it is given. pnpm does not hoist: a dependency lives in the CONSUMING package's own `node_modules`, so starting at the repo root finds nothing, the deliberate unknown-is-true rule turns that into "keep reporting", and the filter suppresses nothing. Measured on n8n: `n8n-nodes-base` has no `exports` field and sits in `packages/@n8n/nodes-langchain/node_modules/`, and `doctor` emitted five deep-import advisories against it. `packageDeclaresExportsMap(root, 'n8n-nodes-base')` → `true`; from the importing file's directory → `false` | [S-09], and a specific recurring sub-form: **a correct function fed the wrong input at its only call site** — the same shape as D051's unused `testFilePattern` parameter and D052's `build-output-excluded` marker tested against the wrong exclude set. Three instances in one week | **The unit tests, which could not.** `deep-import-no-exports.test.ts` had six cases and all six passed, because `packageDeclaresExportsMap` was correct the whole time — the defect lived entirely in what `doctor` passed it. The wiring test I added alongside it (D055's own "a correct predicate nobody calls is D051's shape") used a ROOT-level `node_modules`, the npm layout, so it exercised the caller and still agreed. **The corpus, not the assertion, was the gap** — and no fixture in this repository has ever had a pnpm strict layout | Resolve from `path.dirname(path.join(rootDir, hit.from))` — the importing file's directory, the way Node itself resolves. Memoized per (directory, package) rather than per package, because under pnpm one name can resolve to different installs in different packages and a per-name cache would let the first lookup answer for all of them. **Measured on n8n: deep-import advisories 161 → 87**, 74 false positives removed. Pinned by a doctor-level test with a genuine pnpm strict layout — the dependency present ONLY in the consuming package's `node_modules`, absent from the repo root — which fails against the old wiring and is the first fixture in this repository of that shape | measured |
| D059 | 2026-08-20 | S2 | Auditing the release procedure before tagging 0.2.1 — reading `.github/workflows/` against ADR 025 §6's own words | **(a) ADR 025 §6 says the cross-version harness is "required before publish", and nothing enforced it.** `release.yml` publishes to npm on `push: tags`; `ci.yml`'s integration job ran on `release: published`. Creating a GitHub Release with a new tag fires BOTH, concurrently — so npm could publish while the harness was still running, or regardless of whether it passed. The one gate the ADR calls mandatory was wired to an event that cannot block the thing it gates. **(b) The manual pre-release check covered one project of two.** `ci.yml`'s `workflow_dispatch` declared `project` with `default: 'nest'`, so a bare dispatch — the "pre-release check on a branch" its own comment advertises — never ran `prune-incomplete-scan-requires-allow-incomplete`, the only integration coverage ADR 023 tier 2 has. The `release: published` path was correct; the path a human would actually use was not | [S-09] for (b): two paths through the same job, one covering every project and one covering the default. For (a), a control attached to the wrong event — the release-process analogue of D048's inert signal handler, where the mechanism exists and cannot fire | **Nothing. No test reads a workflow file**, and the harness cannot check its own invocation. Both were found by reading the YAML against the ADR sentence it claims to implement — the same instrument that found D049, one process level up. Three release-gate defects (D012, D049, D059) have now all been found by a human reading the gate's definition rather than by anything executable, which is itself the measurement: this project has no instrument pointed at its own release procedure | (a) The harness now runs INSIDE `release.yml`, after the unit gate and **before** the publish step, via `pnpm integration:release` (which covers every project since D049) — verified by parsing the workflow and asserting the harness step index precedes the publish step index, rather than by reading it. Results upload on `always()` with 90-day retention so a published version's gate evidence stays inspectable. The `release: published` trigger is REMOVED from `ci.yml` rather than left in place: keeping it would re-run the same 25-minute gate after the fact, where it can block nothing. (b) `project` input defaults to `''`, which the existing `if` conditions already treat as "every project" | measured |
| D057 | 2026-08-20 | S2 | The same field report as D052/D054/D055/D056 — a user whose brief was literally "the chrome extension cannot import node modules", who read `align skill --topic authoring` in full and found nothing | **`external()` is fully implemented, builtin-aware, works perfectly, and appeared in ZERO shipped guidance.** Measured occurrences of `external(` on 2026-08-20: `skill --topic authoring` 0, `skill --topic all` 0, `docs config` 0, `docs verbs` 0, `docs rules` 0. Worse than absent — the verb table described `cannotDependOn(...refs)` as taking *"the listed components"*, which actively implies an external target is impossible. **Cost, measured in the reporter's own work:** they wrote a ~60-line `custom.host` predicate over `externalNodes`/`externalEdges` instead of `cannotDependOn(external('node:*'))`, then deleted it when told the feature existed. A capability nobody can find is indistinguishable from one that does not exist | [S-13] a defect that disables the instrument that would find it — **fourth instance, and the first where the instrument is a completeness GUARANTEE rather than a search tool** | **`dsl-verb-manifest.test.ts`, which exists to guarantee exactly this and structurally cannot.** `introspectVerbPaths()` enumerates `Object.keys()` of the live builder objects, so it sees builder METHODS. `external()` is a standalone export passed AS AN ARGUMENT — outside what method introspection can reach, by construction. The guard's blind spot is precisely the defect's habitat. (Noted in passing: `arch.noCycles` was already hand-pushed into that list, so the "live registry" was partly hand-maintained before this) | `external()` documented in the verb manifest, which feeds `align skill --topic authoring`/`all` and `docs verbs` (measured 0 → 3 each); `arch.no-dependency`'s rule-kind reference now says the target may be an external package (`docs rules` 0 → 1). **The durable half is a new invariant that asks a different question**: not "does every builder METHOD have a description?" but "does every CALLABLE importable from `@spikedpunch/align-core/dsl` have one?" — the actual public surface, since anything importable can appear in a config. `dsl-argument-factories.test.ts` walks the entrypoint's real exports against a classified exemption register with reasons. It found `external` AND `ruleBuilder` on its first run, and rejected two exemptions I had guessed at that are not exported — the register was corrected from what the test measured, not from what I assumed. Mutation-checked | measured |
| D056 | 2026-08-20 | S3 | The same field report as D052/D054/D055 — a user who hit BOTH directions in one session and noticed the contradiction | **`import type` is invisible to `arch.no-cycles` and unconditionally fatal to `arch.layers`, with no opt-out and nothing in either message saying which kind of edge was caught.** align accepts `import type` as the FIX for a cycle — the reporter used it eleven times — and the same erased edge is a hard layering violation. Verified in `evaluators.ts`: `arch.no-cycles` gates on `includeTypeOnly` (RUNTIME_KINDS by default), the EXTERNAL arms of `no-dependency`/`layers` gate on the selector's `includeTypeOnly`, and the INTERNAL arms of both iterate `graph.edges` with no `edge.kind` filter at all. **Three of four arms had the option; the fourth is the one a user meets first** | [S-09] | **Nothing, and the two existing tests are why it looked fine.** `evaluators.test.ts` has "excludes type-only edges by default" and "includes type-only edges when includeTypeOnly is true" — both against `arch.no-cycles`, both passing, both true. A reader checking whether align handles type-only edges finds coverage and stops. Nothing tested the same question against the internal layering arms, because nothing suggested they answered it differently | **The default is KEPT and the reporter agrees it is arguably right**: a type-only import is a compile-time coupling across a boundary even though it erases at runtime, so layering counts it and cycles do not. The defect was the asymmetry and the silence, not the strictness. Both internal arms now share one `excludedAsTypeOnly` filter; `includeTypeOnly` is OPTIONAL on both IR schemas with `undefined` meaning INCLUDE (required would fail validation on every `.align/ruleset-ir.json` an earlier align wrote); the DSL exposes it as `arch.layer(x, { includeTypeOnly: false })` — on `layer()` rather than the variadic `...refs` verbs, and exposed at all because an IR field no config can set is a seam wired to nothing [D051]; and the violation carries `edgeKind` so both messages name it and say how to opt out. Making `edgeKind` REQUIRED found all five hand-built fixtures across three packages. Verified both directions on the reporter's fixture 04. Pinned by `type-only-layering.test.ts` (8), including a pin that no-cycles keeps its OPPOSITE default so nobody "harmonises" them later, and an id-stability pin since the option must never re-identify an accepted violation; filter mutation-checked | measured |
| D055 | 2026-08-20 | S3 | The same field report as D052/D054 — a user reading `align doctor` output against a dependency they knew well | **The deep-import advisory made a claim that was false by construction, twice over, about one specifier.** `import 'reactflow/dist/style.css'` produced *"reaches past reactflow's public surface via 'dist'"*. (a) It is a **stylesheet** — align's own scanner had already classified it `uncertain` with `reason: 'asset-specifier'`, and `computeDeepImportHits` flattens each marker to `{file, specifier, line}`, discarding the reason ONE LINE before asking the question that reason answers. Measured on the reporter's fixture: the specifier appears in no edge collection and exactly one uncertainty marker carrying that reason. (b) reactflow declares **no `exports` field at all** (`main: dist/index.js` only), so under Node resolution every subpath is public — `dist/style.css` is its documented stylesheet path and there is no boundary to reach past | [S-03] a claim asserted about code the checker never consulted — the answer was already computed and thrown away | **Nothing, and for (b) the ADR is the reason**: ADR 020 measured Arm-B (full `exports`-map subpath resolution) as not worth its cost, and `deep-imports.ts`'s header records that as "no manifest/exports resolution". A correct decision about the expensive question left the cheap one unasked. For (a) there is no design reason at all — the check simply discarded a classification it already had | (a) Skip `asset-specifier` occurrences, filtered on the REASON rather than the file extension: `graph.uncertain` is a legitimate source of deep-import occurrences, so dropping the collection would narrow the check far past the defect. (b) `packageDeclaresExportsMap` in the CLI (core does no I/O — enforced executably), memoized per package name, walking `node_modules` UPWARD because hoisting means a root-only lookup answers "not found" for most monorepos. **Explicitly not Arm-B, recorded as an amendment rather than a quiet reversal**: this asks one bit, *does a declared surface exist*, at one manifest read per distinct package — it deletes a class of false claim instead of refining true ones. **Unknown answers TRUE**, deliberately: this predicate gates an FP filter, so answering `false` for an unreadable package would suppress every deep-import advisory in a repo with no `node_modules` — turning an FP fix into an FN. Pinned by `deep-import-assets.test.ts` (3) and `deep-import-no-exports.test.ts` (8, predicate AND the `runDoctor` wiring in both directions, because a correct predicate nobody calls is D051's shape); both mutation-checked | measured |
| D054 | 2026-08-20 | S2 | The same field report as D052, from a user authoring a 42-rule ruleset — noticed because the violation count refused to fall while they were demonstrably fixing cycles | **`arch.no-cycles` reports one cycle per strongly connected component and never said so, so the count reads as a work estimate and progress reads as zero.** `evaluators.ts` runs `tarjanScc`, takes ONE chain per SCC via `extractCycleChainNodes`, and emits one violation — but an SCC can hold many distinct cycles. Reproduced on the reporter's fixture and independently here: `a1<->a2`, `b1<->b2`, `b1<->b3` is three cycles in two SCCs, and align reports **two**. Fix the reported `b1<->b3` and `b1<->b2` takes its place, still two. In their repository align said **12 and it took 17 distinct fixes**, with the number static across most of them — "12 → 12" after a day of real work | [S-03]-adjacent: the output asserts a complete finding ("Import cycle of 2 edge(s) detected: ...") about a representative sample, with nothing in the message marking it as one | **Nothing, and no test could have.** Every existing cycle test uses a fixture whose SCC contains exactly one cycle — the case where representative and whole coincide — so the assertions are all true and the gap is invisible. The defect only appears at an SCC with two or more distinct cycles, which no fixture had | **One-per-SCC KEPT**, and the reporter agrees it is defensible: enumerating every cycle in a component is exponential, and an SCC genuinely is one architectural problem. What changed is that the violation now carries `cycleGroup` (the SCC's members) and the message names them when the group is larger than the cycle shown: *"These 3 files form one mutually-dependent group (...); align reports one cycle per group, so breaking the cycle above may leave other cycles among these files."* **A message change and not a selection change, for a hard reason**: the violation id is `computeFingerprint(['no-cycles', ruleId, ...chain])` — the chain IS the identity, so reporting a different or additional cycle re-identifies every baselined cycle entry in every repository using align. A two-file cycle, which is the overwhelming majority, stays plain [S-04]. Pinned by `cycles-one-per-group.test.ts` (3), including an exact-id assertion so a future change to selection or fingerprint inputs fails there instead of silently churning baselines; the group-note condition is mutation-checked | measured |
| D053 | 2026-08-20 | S1 | **The D052 safety net, on its first run** — characterizing the 820 edges that still pointed at non-nodes after the resolution fix, expecting one bucket and finding two | **A legitimate SOURCE directory named `build`, `dist`, `out`, `coverage` or `.cache` is silently dropped from the scan, at any depth.** `DEFAULT_EXCLUDED_DIR_NAMES` is matched against every directory ENTRY NAME during the walk, not against build-output locations, so the exclusion meant for `<pkg>/dist` also swallows `src/build/`. Measured in align's OWN repository: `packages/core/src/build/` holds **14 TypeScript source files and zero are scanned**, so align's architecture rules have never evaluated any of them and its self-dogfood green verdict does not cover them. ADR 028 records the blind spot (`default-excluded-dir`), but that reason is deliberately advisory-free — you wrote the pattern, every repo has a `node_modules` — so nothing is printed. The 820-edge census bucketed 818 `dist` + 2 `build`; the two are these files | [S-06] a path-based configuration silently widened by a name collision — the exclusion targets a ROLE (build output) and is implemented as a NAME, so any directory that happens to share the name inherits it | **Nothing, and ADR 028's own reasoning is why.** The advisory-free carve-out for `default-excluded-dir` is correct for its intended case and hides this one, because the two are indistinguishable at directory grain: `packages/core/src/build` and `packages/core/dist` produce the same blind-spot record. The D052 edge-grain advisory is the first instrument in this project that could see the difference, and it found it on its first run | **FIXED 2026-08-20**, after one batch carried as identified-not-fixed. `DEFAULT_EXCLUDED_DIR_NAMES` is split by ROLE: `ALWAYS_EXCLUDED_DIR_NAMES` (`node_modules`, `.git`) stays any-depth — nested installs are real and a nested `.git` is a checkout boundary; `BUILD_OUTPUT_DIR_NAMES` (`dist`, `build`, `out`, `coverage`, `.next`, `.turbo`, `.cache`, `.build`, `.history`) applies only at a package root, beside the `package.json` that declares the output, or at the repository root. **The discriminator is free**: the walk already holds `readdirSync(…, {withFileTypes: true})` for the directory it is visiting, so "does this contain a package.json" is a lookup in data already read, not another `stat`. Rejected as more machinery than the measured defect justifies: `tsconfig` `outDir` (more precise, needs the resolver during the walk) and `.gitignore` (whose negation syntax align's glob dialect cannot express). The D052 remap consults the UNION via `mayBeExcludedFromScan`, documented as deliberately conservative — the resolver holds a path, not the listing that produced it, and over-answering yes is safe because the remap only fires for a workspace-package specifier, where the inventory's answer is the source entry either way. **Measured after the fix on align's own repository: 14 of 14 `packages/core/src/build` files now scanned (was 0), 6255 nodes, `align check` still GREEN at debt 29 → 29**, and the `unevaluatable-edges` advisory that found this dropped from 5 targets to 1. That advisory's own wording claimed align skips these names "at any depth" and was corrected in the same commit — it would have been stale the moment this landed. Pinned by `build-name-source-dirs.test.ts` (5); position check mutation-checked | measured |
| D052 | 2026-08-20 | S1 | **A user's own experiment, and the best-instrumented report this project has received**: green looked wrong, so they emptied 40 allowlists to `canOnlyDependOn()` and got 9 violations from 2 components. Then hid `libs/core/dist` — 62 edges appeared; restored it — 0. The two working components were exactly the two packages with no `node_modules` installed | **Every cross-package edge in a BUILT, INSTALLED workspace was invisible to every rule, and align reported green.** Chain, verified line by line: `ts.resolveModuleName` succeeds because the package is built (`main: dist/index.js` has a real `dist/index.d.ts`); the workspace-inventory fallback is gated on `resolved === undefined` so it never runs — its own comment names the sibling case, *"whose declared entry is a NOT-YET-BUILT `dist/`"*; realpath lands inside the repo and outside `node_modules`, so the target is correctly INTERNAL at `libs/core/dist/index.d.ts`; the non-source guard does not fire because `path.extname('index.d.ts')` is `.ts` (its comment claims it filters `.d.ts` — it does not); the `build-output-excluded` uncertainty — a reason that exists for exactly this — does not fire either, because it tests the USER's `excludes` and never `DEFAULT_EXCLUDED_DIR_NAMES`; the walk skipped `dist/`, so the target is not a node; and every evaluator opens with `if (fromNode === undefined \|\| toNode === undefined) continue`. **The reporter's sharpest observation, and the reason this outranks an ordinary false green: the gate goes quiet exactly when the dependency becomes real.** Add a dependency and there is no symlink yet, so `ts.resolveModuleName` FAILS, the workspace fallback fires, and the rule correctly catches the violation. Then `pnpm install` creates the symlink, resolution starts succeeding, and the violation disappears. So align is loudest about a dependency that does not exist yet and silent about the one that does — an accidental partial mitigation that actively teaches a user the rule works. **Measured in align's own repository: 44 of 44 `cli/src -> core/*` edges pointed at non-nodes.** align's `cli.canOnlyDependOn(...)`, `agent.canOnlyDependOn(core)` and `core.cannotDependOn(pluginTypescript, cli, agent)` — its foundational rule — could not fire, and its CI self-dogfood has been green on that basis | [S-09] | **Nothing, and the fixture corpus explains why.** Zero fixtures in this repository contain a `dist/` directory or a `node_modules` symlink to a sibling package — because `.gitignore` ignores both globally, so a fixture of the real shape CANNOT be committed. The entire suite exercised the resolution-FAILS path; the resolution-SUCCEEDS path had no coverage at all. The user's own observation names it exactly: the only components that worked were the ones with nothing installed | Option A of two, both built and measured before choosing: TS stays authoritative EXCEPT when its internal answer lands in a default-excluded directory, where the workspace inventory remaps to the package's source entry. Option B (inventory first) produced identical numbers on both real corpora — 44/44, 18191 edges, 820 dangling, 6727 uncertain — so a divergence fixture decided it: with `tsconfig paths` pointing at a non-default entry, A honours it (`alt-entry.ts`) and B overrides it (`index.ts`), which matters for `arch.metric`, host predicates and every violation location. A needed `DEFAULT_EXCLUDED_DIR_NAMES` from `scanner.ts`, which imports the resolver — **align's own `arch.no-cycles` caught the import cycle I introduced**, fixed by extracting `scan-scope.ts` (copied verbatim by script, not retyped). **Plus the safety net, which is the part that transfers**: `buildUnevaluatableEdgeAdvisories` reports any edge whose target is not a node, on every surface that reports blind spots — the resolution bug was the cause, the SILENCE is what let it survive review, a release and the self-dogfood. **Not a 0.2.0 regression — every published version carries the identical code**, verified against each release tag (0.1.1/0.1.2/0.1.3/0.1.4/0.2.0 all contain the same `if (resolved === undefined)` gate in a 153-line file). So no downgrade is a remedy and 0.2.1 is the first release that resolves this. Establishing that took two attempts: `grep -c` in this shell is ugrep and returned empty and then `0` for a file that demonstrably contains the line, which would have inverted the conclusion — re-derived in Python (CLAUDE.md §1.2). Confirmed against the reporter's own fixture on 2026-08-20, both halves on the same tree: published 0.2.0 reports **green** as shipped and red with `dist` hidden; the fixed build reports **red** in all three configurations (as shipped / dist hidden / symlink removed). Pinned by `built-workspace-resolution.test.ts` (3, fixture built in a tmpdir since the shape is uncommittable) and `unevaluatable-edges.test.ts` (5); both mutation-checked | measured |
| D051 | 2026-08-20 | S2 | **A user running `align agent run` on a real monorepo after upgrading to 0.2.0** — twelve files across seven packages all escalated with the same line and no agent session ever started. The first field report this ledger has from outside align's own repository | **`align agent run`'s coverage gate reported "this file is untested" when the truth was "align saw no test files at all".** `isFileCovered` returned `false` the moment `testFiles.length === 0`, so a scan matching zero test files marked EVERY file uncovered, and the gate — which runs before `fixProvider.proposeFix` — escalated every group without planning or sending anything. The message read *"no scanned test file transitively imports this file (pass --allow-untested to override)"*, which asserts tests were scanned and points the user at writing tests they may already have. The two real causes are configuration: an `excludes` pattern hiding the test tree, or a naming convention outside the hardcoded `/\\.(test\|spec)\\./`. **Third defect, same function:** `isFileCovered` had accepted a `testFilePattern` parameter since it was written and NO caller ever passed one, so a repo using `__tests__/` or `.e2e.` had no way to say so | [S-10] absence treated as evidence — an empty test-file set is "I could not look", and the honest answer to "is this covered?" from a scan that saw no tests is *unknown*, not *no* | **Nothing, and the agent package's own README documented the heuristic accurately while missing this**: it explained reachability-not-coverage and the false-confidence direction, and never asked what `false` means when the test set is empty. The unit tests pinned the behaviour rather than questioning it — `coverage.test.ts`'s fourth case asserts `isFileCovered(...) === false` for a graph with no test files, so the defect had a test guarding it. An unused parameter recorded the intent and enforced nothing [S-02]-adjacent | `assessCoverage` returns `{covered, scannedTestFileCount}`; the gate branches on the count into two reasons — one naming the count for the real per-file finding, one saying "cannot tell" and naming `excludes`/`testFiles` for the config case. **Both still refuse**: "align could not look" is not a licence to edit unverifiable code. Patterns come from a new `testFiles` config export in align's glob dialect (linted by D044's `readGlobPatternArrayExport`, and added to that register's table in the same commit rather than after someone noticed), defaulting to the two globs that reproduce the old regex — asserted file-by-file against it, since a silent widening changes the verdict for every repo that configures nothing. `[]` is spread away at the CLI boundary so an absent export means "defaults", never "no tests exist" — that would be this defect reintroduced by its own fix. Pinned by `coverage-cannot-tell.test.ts` (6) and `coverage-refusal-reason.test.ts` (3); both halves mutation-checked. Two pre-existing `run.test.ts` cases used an empty graph to mean "uncovered" and now use a scanned-but-unrelated test file, so they exercise the branch they were written for | measured |
| D050 | 2026-08-20 | S1 | A release-readiness review, prompted by the user asserting we were ready to tag 0.2.0 — not by any test | **Phase 5 shipped seven consumer-visible behaviour changes with no `UPGRADING.md` entry for any of them, and one is breaking.** `git log -1 -- UPGRADING.md` pointed at `370bc83`, a pre-phase-5 commit; `grep -ic 'extglob\|align_violations\|errorMessage'` returned 0. The breaking one is D044: an `excludes` / `includeNestedCheckouts` / `knownPublicDeepImports` pattern using unsupported glob syntax was silently inert and now fails config load, so a repository whose config loaded yesterday has every command refuse today — with no note saying why or what to write instead. Also unrecorded: a SECURITY fix (D041, code execution via a directory name in `align init`), an MCP contract change (D047, `align_violations` no longer answers an errored scan with `[]`), a payload-semantics change (D043), a new advisory (D042), and `align init` silently skipping directories (D045). A seventh, D046, made an EXISTING note wrong by understatement — it still said atomic writes covered `.align/` only | [S-03] a doc asserting a fact about code it does not govern — the atomicity note — plus a plain process miss for the other six | **`RELEASING.md` states the rule and nothing enforces it**: *"record it in `UPGRADING.md` in the same commit as the change, not at release time."* `migration-notes-drift.test.ts` enforces that `notes.generated.ts` matches `UPGRADING.md`, so the artifact pair cannot drift — but nothing ties a behaviour change to the existence of a note, and nothing can, mechanically. The only instrument that found it was reading the release checklist against the diff | Seven sections authored and compiled (`node packages/cli/scripts/compile-upgrading-notes.mjs`), leading with the breaking change: the exact error text, the rewrite, and the warning that a newly-effective exclude changes what a scan sees and therefore what `prune` will offer to delete. The D046 note was widened rather than added to. Verified at the surface a user actually reaches — `align upgrade --notes --from 0.1.4` now lists all five new headings, 29 notes total. **Not promoted to an invariant, deliberately**: "every behaviour change has a note" is not decidable from the diff, and a check that approximated it would either pass vacuously or block every commit. The control that exists is the release-readiness review this row records | measured |
| D049 | 2026-08-20 | S2 | Auditing the release procedure before tagging 0.2.0 — reading `package.json`'s scripts against CLAUDE.md's documented gate | **The script named for the release gate never ran the release gate's tenth calibration scenario.** `integration:release` was `node integration/run.mjs --targets 0.1.4,local`, and `run.mjs` takes ONE `--project` (default `nest`) and filters its scenario pool by it — so `prune-incomplete-scan-requires-allow-incomplete`, which declares `project: 'nest-incomplete'` and is the only integration coverage ADR 023 tier 2 has, was invisible to it. This is D012's finding, verbatim: *"a release-gate scenario the release-gate command does not execute is not calibration"* | [S-09] | **D012 itself, which recorded this as FIXED.** Its Fix column reads *"scenario passes `--yes`; CLAUDE.md's verification block now runs both projects, and says why"* — the scenario was fixed and the gap was documented, but the COMMAND was not changed, so the mitigation was a sentence a human had to remember. The failure mode is silence: it passes whenever you do remember, so three days of running both lines by hand produced no signal at all. A fix that converts an executable gap into a documented one has not closed it | `scripts/integration-all-projects.mjs` runs `run.mjs` once per project and fails if any fails; `integration:dev` and `integration:release` both go through it, and CLAUDE.md's verification block collapses to `pnpm integration:dev`. The project list is **discovered** from `integration/projects/`, never enumerated — a hardcoded list would reproduce the defect one level up, which is exactly how the second project escaped. Guards: passing `--project` is refused with the reason, and a discovery finding zero projects exits 2 rather than reporting a pass over an empty set. A signal-killed child (`status: null`) counts as a failure, the D048 lesson one process up | measured |
| D048 | 2026-08-20 | S2 | A user's question — "do the matrix run scripts handle crash scenarios correctly?" — after three of my own background matrix runs were killed. The kills were environmental (my task runner, not the harness); asking the question anyway is what found the two real defects | **(a) The harness leaked 21 GB of working copies, because `expectFailOn` was consulted by the calibration check and not by the preserve-on-failure decision.** `run.mjs` preserves the working tree of any non-passing scenario for diagnosis (ADR 025 §2) — including a scenario PINNED to fail on `0.1.4`, where the failure IS the calibration succeeding. Measured 2026-08-20: `results/.cache/work` held 39 directories totalling 21 GB, 9 of them from a single 9-scenario calibration re-run in which every failure was the one being deliberately checked for. Nothing ever reaped them. **(b) The whole verdict was lost on any interruption.** The summary, `summary.json`, the gate check and the release-blocking calibration check were all inline at the end of `main()`, so the most important assertion the harness makes was the last thing to execute and the first thing lost — measured as 16 result directories with no `summary.json`, and a calibration verdict I had to reconstruct by hand from PASS/FAIL lines in a truncated log. There were no signal handlers anywhere in `integration/`, so F12's cleanup comment claiming it removes the tarball cache dir "unconditionally (success or failure)" was true of exceptions and false of every Ctrl-C — 8 leaked directories | [S-09] for (a): one arm of the harness knows a pinned failure is expected, the other treats it as a surprise. **[S-13] for the near-miss in (b)'s own fix** — see the Fix column | For (a), nothing: no check in this project looks at its own harness's disk use, and the preserve rule reads as obviously correct in isolation. For (b), nothing either — an interrupted run's exit code is the signal's, so it never claims success, and the missing summary was the only (implicit) evidence anything was wrong | (a) The preserve decision now consults `expectFailOn`; a harness ERROR is still preserved even when pinned, since the pin covers failing assertions, not the harness blowing up. (b) `finishRun({complete})` extracted and called from SIGINT/SIGTERM handlers; `summary.json` gains `complete`/`ranScenarios`/`expectedScenarios`, and an interrupted run refuses to print "all scenarios passed", exiting non-zero even when nothing failed. **The near-miss, and the reason (b) is one commit and not two:** the handler as first written was INERT. Every step runs through `spawnSync`, so `await runScenario(...)` yields only to the microtask queue and libuv never reaches the phase that delivers a queued signal — and a registered handler suppresses the default terminate, so adding it turned a working Ctrl-C into one that does nothing at all. Reduced to a 20-line reproduction: an async loop of `execSync('sleep 3')` ignored SIGINT and exited 0; the same loop with one `await new Promise(r => setImmediate(r))` exited 130. Caught only by measuring the handler instead of trusting its registration. Verified end to end after the fix: SIGINT → exit 130, SIGTERM → exit 143, both writing `complete:false, ranScenarios:2, expectedScenarios:4` and leaving 0 work and 0 tarball dirs; a clean run still exits 0 with `complete:true` | measured |
| D047 | 2026-08-19 | **S0** | Reading D043's own fixed output — the payload said `verdict: "error", complete: false` and nothing else, which raised "so what broke?"; probing the sibling MCP tool for the same question found the severe half | **`align_violations` returned `{"violations": []}` with `isError: false` for a scan that evaluated no rule at all** — byte-identical to the answer for a clean repository. Measured against the real MCP server over an in-memory transport, on `simple-app` with one selector repointed at a missing path. `align_check` at least reports `verdict: "error"` beside its empty list, so a careful agent can branch; this tool returns ONLY the list and gave it nothing to branch on. Absent means "never verified", never "none". **The milder half, same fix:** `GateResult.errorMessage` holds the whole diagnosis (component, selector, three remedies) and the human surface prints it, while the payload's gate projection dropped the field — so `align check --json` exited 1 with the reason on NEITHER stream, stderr empty, and `align_check` told an agent something broke without saying what | [S-09] | Nothing, and the near miss is a comment that reads as a decision: `gates/types.ts` marked `errorMessage` *"environmental, never LLM-facing"*, and ADR 007 bans per-item text from PASSING gates. Both were reasonable defaults nobody had priced — an errored gate is not a passing gate, `advisories` already carry free prose into the same payload, and the bound is one string across at most three gates. The exclusion survived because it looked deliberate | `errorMessage` now on the payload's errored gates (spread, so a passing gate carries no key at all — ADR 007's ban stands). `align_violations` refuses with `isError: true` on `verdict === 'error'`; a `{violations: [], error: ...}` hybrid was rejected because it leaves the misreadable `[]` for any caller reading the field it asked for. RED runs are deliberately NOT refused. **`describeErroredGates` extracted into core as the one formatter** — `errored-run.ts` and `packages/agent` had already grown identical copies down to the same dead `\|\| 'a gate errored'` fallback, and this needed a third; that fallback was unreachable in both and is dropped rather than propagated. Pinned by `core/test/errored-run-names-its-reason.test.ts` (6) and `cli/test/mcp-errored-run.test.ts` (5, incl. RED and GREEN calibrations that the refusal does not fire). Both halves mutation-checked | measured |
| D046 | 2026-08-19 | S1 | Bug hunt, FRAGILE F1 — the report named the asymmetry; the census of who is on which side was taken by grep in this session | **Every file align writes into someone else's repository was truncated in place, while every file align owns was replaced atomically.** `fs-atomic.ts` exists specifically because `fs.writeFileSync` opens with `O_TRUNC` and a process killed in that window leaves a truncated or empty file; its header argues the case at length — for `.align/`. Enumerated: eight call sites still on bare `fs.writeFileSync`, writing `CLAUDE.md` (x2), `align.config.ts` (init, the note block, and the glob migration), `.gitignore`, `package.json`, `.claude/skills/*`, and the user's SOURCE files during `align agent` repair. The inversion is the point: `.align/baseline.json` is align's own and recoverable by re-accepting; `CLAUDE.md` and `package.json` are the human's and align cannot regenerate a line of them. **Confidence note, so nobody quotes this wrongly: the call-site census is measured, the truncation is INFERRED from `O_TRUNC` semantics — a crash window cannot be reproduced in-process, and no test here claims to.** | [S-09] | Nothing. ADR 030 §"what this does not do" enumerates only `.align/` paths, so the question was never asked outside them; CLAUDE.md rule 3 governs WHAT align may write to the two shared files and is silent on what a crash mid-write leaves. `fs-atomic.ts`'s own header said "everything align persists under `.align/`" — accurate as a description of the callers, which is exactly why it read as a scope decision rather than a gap | All eight routed through `writeFileAtomic` (which already handles the two hazards a naive temp+rename drops: mode, and following a symlink to its target). Header corrected. **Promoted to an executable invariant** — `cli/test/writes-are-atomic.test.ts` enumerates every direct `fs.writeFileSync`/`appendFileSync`/`createWriteStream` under `packages/cli/src` and fails on any not in a two-entry exemption register carrying its reason (`align-lock.ts`: it IS the atomicity mechanism; `align-dir.ts`: telemetry APPENDS, which does not truncate). Mutation-checked: reverting `gitignore.ts` fails the invariant naming the file | measured |
| D045 | 2026-08-19 | S2 | Measuring D044 — running the control case ("the same pattern as a component selector errors loudly") and reading the output instead of assuming it (CLAUDE.md §1.2) | **The glob-dialect lint was unreachable for the default `empty` policy, so the precise message its own doc comment promises was never the one a user got.** `validateSelectorSyntax` says unsupported syntax fails "at the exact pattern, with an actionable message, instead of at scan time as a mysterious zero-match error". Measured with `components: { api: 'src/+(api\|legacy)/**' }`: `parse ERROR — Component 'api' ... matches zero files. Likely cause: its directory was renamed/moved or the selector is stale.` The mysterious zero-match error, verbatim, plus a remedy sending the reader to hunt for a directory sitting exactly where they left it. Cause is ordering: `validateComponents` runs in the PARSE gate (the plugin scanner calls it), `validateSelectorSyntax` in the ARCHITECTURE gate, which never executes because parse already errored — and an unsupported construct compiles to a literal matching nothing, so the coarse check ALWAYS wins the race. It worked only for `empty: 'allow' \| 'until-populated'`, where the zero-match branch is skipped — the case its comment treats as the afterthought | [S-09] | Nothing, and the near miss is instructive: `core/test/components-registry.test.ts` calls `validateSelectorSyntax` DIRECTLY and passes whether or not anything ever reaches it. A test that proves a function works is not a test that proves it runs. CLAUDE.md rule 5 again — the comment described a guarantee the call graph did not deliver | `validateSelectorSyntax(components)` now runs first inside `validateComponents` — syntax before semantics, at the same moment, changing nothing about WHEN validation happens (pure and idempotent, so the orchestrator's own call still costs nothing). Pinned by `core/test/selector-lint-not-shadowed.test.ts` (4), including the calibration that a genuinely stale selector still says "renamed/moved". **Second-order consequence, fixed with it:** the lint became reachable from `align init`'s probe scan, which turned a legal directory name like `docs (old)` into an uncaught crash out of `runInit`. `init` now partitions detected components by whether the dialect can express them at all, drops the ones it cannot, SAYS which, and refuses cleanly if that leaves none | measured |
| D044 | 2026-08-19 | S2 | Bug hunt, FRAGILE F3 | **Three config exports carrying glob patterns were linted by nothing, while the fourth pattern source was linted hard.** `excludes`, `includeNestedCheckouts` and `knownPublicDeepImports` are all matched by the same `globMatch` as component selectors, and `globToRegExp` ESCAPES every metacharacter outside align's minimal dialect — so an unsupported construct neither errors nor approximates, it compiles to a literal that matches nothing, silently, forever. Measured on a repo with an api→ui violation and `export const excludes = ['src/+(api\|legacy)/**']`: `align check` printed the violation the exclude was written to suppress, exit 1, no mention of the pattern anywhere. **Direction stated plainly: all three fail SAFE for the verdict** — an inert `excludes` scans more, an inert `includeNestedCheckouts` scans less but records an ADR 028 blind spot, an inert allowlist reports an advisory it was told to suppress. None is a false green, which is why it was FRAGILE and not S1. What is wrong is the asymmetry: a user who learned the dialect from yesterday's error message gets no message today | [S-09] | Nothing. The lint lives in `components/registry.ts` and is shaped around a `Record<ComponentName, ComponentDefinitionIR>`, so it was structurally unavailable to a bare `string[]` — the coupling to the component type is why the sibling inputs were never candidates | `readGlobPatternArrayExport` in `cli/src/config.ts` lints all three at config load (before the scan, since two of them shape what the scan looks at), reusing `lintGlobPattern` and the now-exported `SUPPORTED_GLOB_VOCABULARY` so the two messages cannot drift into describing different dialects. `compositionRoots` deliberately NOT linted — those are component names resolved by `compositionRoots.has(from)` in `computeUngovernedEdgeGaps`, read rather than assumed, and the register test says so. Pinned by `cli/test/config-glob-dialect-lint.test.ts` (6): one per export from a table, the CLI-visible path, and two calibrations (every supported construct still loads; `compositionRoots` still accepts a name) | measured |
| D043 | 2026-08-19 | S2 | Bug hunt, FRAGILE F2 | **An errored run reported `complete: true` on the wire.** `untrustworthyScanScope()` forces FOUR sibling fields to their knows-nothing value on every errored early return, precisely so nothing reads a completeness claim off a run that has none. `complete` is the fifth field answering that question, and it was outside the set because it is DERIVED (`isRunComplete`) rather than stored — invisible to the `Pick` whose whole job is to make the compiler enumerate those returns. An errored run satisfies both existing axes trivially: no `missing-dependencies` advisory fired and `ungroundedComponents` is `[]`, because nothing got far enough to look. Measured, `align check --json` on a repo with a stale selector: `"verdict": "error", "complete": true`, every other field honestly zeroed. Four shapes reach it, not three — a manifest scanner that throws errors the SECURITY gate while the architecture pipeline runs to completion, so that run reaches the ordinary return with a real scan scope | [S-09] | Nothing directly, and the sibling guard is the near miss: ADR 023 tier 1 (`refuseIfRunErrored`) precedes tier 2 at both destructive call sites, so no errored run has ever reached the overridable guard — which is why the predicate's wrong answer had no consequence anyone could trip over, and stayed | Third axis added to `isRunComplete` (`run.verdict !== 'error'`), in the predicate rather than at the payload builder: a payload-level patch keyed off the zeroed fields would have missed the security-gate shape and every non-payload consumer. Strictly stronger in both directions — a future site calling only tier 2 now refuses (overridably) where it used to pass. Tier 2 gained an errored branch for its message, so it names the right axis if it is ever reached alone. Pinned by `core/test/errored-run-is-not-complete.test.ts` (10): all four errored shapes built THROUGH the orchestrator rather than hand-written, x2 for predicate and payload, plus green-is-complete and red-is-complete calibrations | measured |
| D042 | 2026-08-19 | **S0** | Bug hunt, Lens 4 (data lifecycle); the severe half surfaced only when the reproduction probe asked what `build --verify` says about the broken state it had just produced | **`align build --apply` left doc-built rules in force with no lockfile, and `--verify` called that fine.** `writeBuildArtifacts` wrote `generated-rules.json`, then `align.config.ts`'s note, then the baseline, then `rules.lock.json`; `loadConfig` merges generated-rules.json on every load regardless. `readBaselineSnapshot` throws on a corrupt baseline and ran AFTER the first write, so measured on `build-app`: `THREW ".align/baseline.json is not valid JSON"` with `generated-rules.json: true`, `rules.lock.json: false`, config note already written — command reports failure, rules are enforced. **The half that outranks it**: `verifyFrozenRules` returned `{"ok":true,"advisories":[]}` for that exact state, because a missing lockfile hit an unconditional early return documented as "a deliberate no-op". Absent read as "no build" when a sibling artifact proves it means "a build whose record is missing" | [S-09] | The function's OWN comment, which asserts this sequence is already atomic — "validated HERE, before any of the three writes, so a malformed align.config.ts fails this whole sequence atomically". True of the one cause it names, false of `readBaselineSnapshot`. CLAUDE.md rule 5 exactly: a doc comment asserting a safety property, treated as evidence. And the guard that should have caught the residue is the verifier, so the check for "are the enforced rules the built rules" was the thing reporting success | Both halves. Writes now run inside ONE re-entrant `withAlignDirLock` with `preflightVersionStamp` and the baseline read hoisted above the first write — closing the corrupt-baseline cause and the another-align-takes-the-lock-between-two-writes cause together. AND `verifyFrozenRules` now emits `generated-rules-without-lockfile` when generated rules exist with no lockfile, because no preflight covers a full disk, an EACCES or a SIGKILL. Pinned by `cli/test/build-apply-atomicity.test.ts` (4, incl. the never-built calibration that keeps `check --frozen-rules` silent on projects that do not use `align build`); both halves mutation-checked | measured |
| D041 | 2026-08-19 | **S0** | Bug hunt, Lens 5 — reported as "writes a syntactically invalid config"; the escalation to code execution came from asking, during the fix, what ELSE a directory name can be when the file being written is TypeScript align then loads | **A directory name became executable code in the `align.config.ts` that `align init` writes.** `render-config.ts` interpolated the component PATTERN into a single-quoted TS literal unescaped, while escaping the component NAME with `JSON.stringify` **on the same line**; the pattern is built straight off a directory name. An apostrophe wrote an unparseable config — with `CLAUDE.md` and `.gitignore` already written, an error naming no file (`Expected ',', got 'ident'`), and no repair on re-run ("align.config.ts already exists — leaving it as-is"). But the file is loaded, so the real exposure is EXECUTION. Measured both directions on the built CLI with a paren-free payload (`a', zz: globalThis.__MARKER__ = 'executed', b: 'c`, chosen because the original IIFE payload trips the glob-dialect lint after the fix and would prove nothing): unfixed → `MARKER value: 'executed' — CODE EXECUTED`; fixed → `undefined`. **Clone an untrusted repo, run `align init`, and the next `align check` runs the repo's code as you** | [S-09] | Nothing — and the guarded sibling is not in another file, it is the SAME LINE. `JSON.stringify(c.name)` and `'${c.pattern}'` sit inside one template literal, one escaped and one not. The other identifier-safety hazards on this path were both found and documented (`sanitizeName`'s `c.kluster-bt` note, `dedupeNames`' `c.foo - 2` note); the string-safety of the value beside them was never asked | `JSON.stringify` on the pattern in both arms of the ternary (the greenfield arm had the identical bug — fixing one would have been an S-09 committed while fixing an S-09). Nothing is rejected or rewritten, only quoted: `"don't/**"` still matches. Pinned by `cli/test/init-config-escaping.test.ts` (7), incl. the marker assertion ordered BEFORE the exit-code assertions — the first draft put it after, and an unfixed align failed on the exit code first, leaving the security property unmeasured while the test still went red | measured |
| D040 | 2026-08-19 | S3 | Reading my own `git diff --stat` output on an unrelated edit and noticing `Bin 10157 -> 12487 bytes` (CLAUDE.md §1.2) | **The bug-hunt report that documents the NUL defect contained a raw NUL of its own**, at the exact spot where it wrote "instead of the escape `\u0000`" — so the file explaining why sources must not be binary to git and grep was itself binary to git and grep, and had been since the commit that fixed D033. Its diffs had been rendering as `Bin ... bytes, 0 insertions, 0 deletions`, including the phase-4 status section added the same day, which therefore landed unreviewable | [S-13] | The executable invariant promoted for D033 — `core/test/sources-are-text-not-binary.test.ts` — which covered `.ts`/`.js` only and explicitly skipped `.agents/`. Both exclusions were wrong for the same reason: this project's reasoning lives in Markdown (`LEDGER.md`, `SHAPES.md`, the ADRs) at least as much as in TypeScript, and CLAUDE.md §1.2's "read your own tool output" is worthless against a file grep skips silently. **A guard scoped narrower than the property it protects is one arm by construction** — S-09's ask, applied to a guard | Byte replaced with the escape. Invariant widened to `.md` and to `.agents/`; swept first — one offender in the entire repository. Mutation-checked: restoring the byte fails the test naming the file, line and byte | measured |
| D039 | 2026-08-19 | S1 | Bug hunt, Lens 5 (error/exception paths), then extended by my own reproduction script — the agent found three crashes, the probe that reproduced them found a fourth shape that does not crash and is worse | **The manifest walker read a shape `JSON.parse` never promised.** `buildManifestRecord` put `JSON.parse` inside a `try` — ADR 028's corrupt-≠-absent discipline — and then read `pkg[field]` and `Object.entries(declared)` OUTSIDE it. `"dependencies": null` (and the same for `devDependencies`/`optionalDependencies`), plus a top-level `null`, threw a raw `TypeError`; `orchestrator.ts`'s gate-level catch turns that into an ERRORED security gate for the whole repository, with a message naming no file, and `blindSpots: []` — discarding every blind spot the walk had already recorded before it reached the bad manifest. **The shape that did not throw is the severe one**: `"dependencies": "oops"` reached `Object.entries` of a string, so align invented four dependencies named `0`,`1`,`2`,`3` and ran `security.manifest.*` rules over them; `"dependencies": []` yielded zero and reported a package that declares nothing. Measured: `THROW dependencies: null`, `THROW top-level null`, `OK dependencies: string  manifests=1 deps=4 blindSpots=0` | [S-09] | Nothing. `workspace.ts` — the sibling walker in the same package, doing the same read — already keeps its field access inside the `try`, and no test compared them. The `unparseable` blind-spot vocabulary this needed already existed and was already wired; only the shape check was missing | `manifestShapeError` validates the parsed value inside the `try` and records `unparseable` with a message naming the offending field. `readLockfile` gained the same check, because an empty `pnpm-lock.yaml` parses to `null` and reported as a lockfile align had read — the exact three-state distinction that function's own header says it exists to hold. Pinned by `plugin-typescript/test/manifest-malformed-fields.test.ts` (10 tests, incl. array/string/`{}`/bare-manifest calibration); mutation-checked (guard disabled → 8 of 10 fail) | measured |
| D038 | 2026-08-19 | S2 | Bug hunt, Lens 8 (cross-implementation divergence) — the guarded sibling is `align init`, whose identical defect was fixed, pinned, AND shipped with a user-facing migration note describing the fix | **`align baseline accept` rewrote the provenance of entries it did not newly accept.** `store.accept` `set`s every violation it is handed unconditionally, and the CLI hands it the FULL current violation set rather than only the unbaselined ones — so every re-run stamped `acceptedAt: Date.now()` and `acceptedBy: 'manual'` over entries accepted earlier by another route, at exit 0, reporting "Accepted N violation(s)" for entries where nothing was decided. Measured end-to-end: `accepted 2023-11-14T22:13:20.000Z (accept-existing)` → `align baseline accept` → `accepted 2026-08-19T22:29:12.644Z (manual)`. `acceptedAt` is the age of a piece of accepted debt and `align baseline show` prints both fields, so debt could never be seen to be old | [S-09] | Nothing, in either direction. `init-seed-provenance.test.ts` pins this exact property for `init` and the shipped migration note (`migrations/notes.generated.ts`) describes it in prose — "an entry a person had accepted manually in 2024 came back looking as though `init` had accepted it moments ago" — while `accept`, the command whose entire job is provenance, had no test asserting either behaviour | `store.accept` carries `acceptedAt`/`acceptedBy` from a prior entry when one exists. **`acceptedValue` is deliberately NOT carried**, and that asymmetry was MEASURED rather than argued: both variants were built and driven through the real CLI (accept at 7 lines, grow to 37, then follow the advisory's own "re-accept to record the new size" instruction). Carried → the advisory is still PRESENT after one re-accept, after two, and after `prune --yes`; current → cleared. Freezing it makes the advisory permanently unclearable by any command align ships, leaving hand-editing `.align/baseline.json` as the only exit. `acceptedAt` is preserved under both variants, so the freeze buys nothing for debt age. Pinned by `cli/test/accept-preserves-provenance.test.ts` (3) and integration scenario `accept-does-not-restamp-provenance`, calibrated `expectFailOn: ['0.1.4']` and **verified failing on 0.1.4** (`got 0` of 18 preserved), not merely assumed | measured |
| D037 | 2026-08-19 | S2 | Bug hunt, two independent lens passes — both flagged that `isConcurrentAlignWriteError` has exactly one consumer | **The MCP surface hard-failed on a concurrent align where both CLI arms deliberately degrade.** `ConcurrentAlignWriteError` exists solely to stop a concurrent align turning a green repository into a failure showing only an error line; `commands/check.ts` routes it to a stderr note and prints results on both the trusted and `--untrusted` arms. `mcp/server.ts`'s `freshCheck` performs the same `writeBaseline` for the same move-transfer with no such branch, so the tool handler's catch-all returned `isError: true` — the agent got a lock-timeout string and NO verdict, no gates, no violations, for a run that had completed green. The transfer was never at risk (the next call re-derives it unconditionally); the ANSWER was | [S-09] | Nothing — and `freshCheck`'s own header predicted it in advance: "this function is the one that keeps getting missed", naming `computeBaselineDebt` ("the third copy the first fix missed") and `withVersionSkew` as the two prior misses. A comment that names a recurring miss is not a control; this is the third instance it warned about, on the surface align ships for agents | `deferBaselineWriteOnConcurrentAlign` turns the refusal into a `baseline-write-deferred` advisory on the run. The CLI says this on stderr; MCP has no stderr an agent reads, so the note had to become payload data. Non-concurrency errors still propagate to `isError`. Pinned by `cli/test/mcp-concurrent-write.test.ts` — one arm holding a REAL lock with a live pid (10s, the production wait), one injecting the refusal to cover the second handler cheaply, one calibrating that a corrupt `version.json` still errors; mutation-checked (branch removed → 2 of 3 fail) | measured |
| D036 | 2026-08-19 | S2 | Bug hunt, Lens 3 and Lens 6 independently — one pass enumerated the damaged-input shapes `readHolder` could receive, the other asked what happens when a loop's exit condition sits on the far side of a `continue` | **The two halves of D029's fix that were still open, both of which brick every writing command in a repository.** **(a) Unbreakable for valid-JSON-but-malformed holders.** D029 taught this file that a lock it cannot understand must still be breakable by age — but it only handled `JSON.parse` THROWING. `{}`, `null`, `123`, `"x"`, a holder missing `pid`, or one whose `acquiredAt` is not a date all parse cleanly and were accepted as valid holders; `Date.parse` then yields `NaN`, `!Number.isFinite(age)` returns false, and the lock is unbreakable **at any age**. `null` was worse — a raw `TypeError` out of the helper rather than the friendly refusal. **(b) An infinite loop.** `breakLock(); continue;` put the deadline check on the far side of the `continue`, so a lock judged breakable but NOT removable — a directory at the lock path, an immutable file — spun forever: `WAIT_TIMEOUT_MS` never fired, one core at 100%, one stderr line per 25ms poll filling a CI log. Measured at 25s against a 10s timeout. | [S-12] a discipline transplanted from the artifact that earned it — **fifth instance, and the second inside this same function**. D029 fixed the reasoning for one input shape and left the same reasoning unapplied to every other shape of the same damage | **D029's own tests, which pin exactly two shapes** — `'not json at all'` and zero-byte — and nothing else; the guarded sibling `readTelemetryState` (`align-dir.ts`) has done the missing shape-validation all along. For (b), nothing: no test had ever made `breakLock` fail, so the `continue` that skipped the deadline was never executed with a lock still in place. | **FIXED 2026-08-19.** (a) `isLockHolder` validates the shape — including that `acquiredAt` actually parses, since an unparseable date reproduces the identical `NaN` hole one field further in — and anything else is "unidentifiable", routing to the age fallback D029 already built. (b) `breakLock` now REPORTS whether the file is gone (checked by re-stat, because `rmSync({force:true})` is silent about a path it could not remove), and the loop only fast-retries on an actual break; a failed break falls through to the deadline and the sleep. The announcement moved after the break, so the sentence describes something that happened rather than printing once per poll while breaking nothing. Measured after: all six damaged shapes acquire in 2ms, the directory case times out cleanly in ~1s. Pinned by 8 tests. **Mutation-checked, and (b)'s check is unusual evidence worth recording: reverting it did not fail an assertion — it HUNG the test runner**, which is the defect. | measured |
| D035 | 2026-08-19 | **S0** | Bug hunt, Lens 8 (cross-implementation divergence) — the guarded sibling was `store.accept`, twelve lines away, doing the right thing for every entry it creates | **`align init` after a rename dropped `contentFingerprint`, and two ordinary renames then destroyed a consent record at exit 0.** The seed merge looked up prior provenance by STRUCTURAL fingerprint, which folds in file identity — so a rename misses the lookup by construction and the field was dropped. An entry with no `contentFingerprint` can never participate in a move-transfer, so the NEXT rename made it an unmatchable orphan and `align baseline prune` deleted it reporting **"Pruned 1 fixed violation(s)"** at exit 0 while `align check` was RED on the violation it had just called fixed. Reproduced twice, once by the lens pass and once independently. `init.ts` asserted the opposite in a comment — *"a violation whose file MOVED keeps its fingerprint"* — while `store.ts` states the truth eleven files away: *"a rename produces a brand-new fingerprint and orphans the old baseline entry by construction"*. | [S-09] fixed one arm, missed the other, plus [S-03]: a comment asserting a safety property nothing implemented (CLAUDE.md rule 5) | **`init-seed-provenance.test.ts`, which pins exactly this merge** — but only for entries whose fingerprint is UNCHANGED. It covers same-fingerprint preservation and genuinely-new stamping; the case where the fingerprint changed is the one the field exists for and the one nobody wrote. No integration scenario renames a source file (`init-rerun-preserves-content-fingerprint.mjs` says in its own comment that its harness cannot). | **FIXED 2026-08-19.** `init` now DERIVES `contentFingerprint` from the violation — `computeContentFingerprint(v.ruleId, v.snippet)`, exactly what `store.accept` already does — instead of carrying it from a prior it may not find. Deriving beats matching here on safety, not just simplicity: a content-match lookup could carry one violation's consent onto another, which is the D015 forgery shape, and deriving cannot. What it does NOT recover is `acceptedAt`/`acceptedBy` across a rename; those still reset, and that is stated rather than guessed at. Pinned by 3 tests, mutation-checked. **The first draft of those tests passed against the unfixed code** — a hand-built fixture exercised the RETAIN path instead of the seed path, and a premise assertion ([S-05]) caught it; the suite now copies the fixture the defect was originally reproduced against and asserts `runInit` returned 0. | measured |
| D034 | 2026-08-19 | **S0** | Bug hunt, Lens 4 (data lifecycle) — following `.align/baseline.json` from creation to deletion, which is the only way to see a two-phase write as one | **A destructive write landed and then reported failure.** Every committed-artifact writer is two-phase — `writeFileAtomic(...)` then `stampAlignVersion` — and the second phase reads `.align/version.json`, which throws on a corrupt one. No concurrency needed: a merge conflict or a half-written stamp is enough. **Measured**: `align baseline prune --yes` with 1 entry in the baseline printed `.align/version.json is not valid JSON` and exited non-zero, and the baseline was left with **0 entries**; the prune report never printed. The user is told the command FAILED, with a message naming a file they did not ask about, while their consent records are gone. `commands/baseline.ts` states the invariant this broke in its own comment — *"a refusal still leaves the baseline file untouched"* — which was true of the ADR 023 refusals and false of this one. Same shape in `writeGeneratedRules`/`writeRulesLock`/`writeRulesetIr`, where `build --apply` leaves doc-built rules in force with no lockfile and `build --verify` then reports the build never happened. | [S-09] fixed one arm, missed the other — `build.ts` had ALREADY closed this window for its own malformed-marker cause, with a comment saying the sequence is atomic; ADR 030 then added two throw sources downstream of the first write and neither was pre-flighted | **`check-concurrent-write.test.ts`, which plants exactly this corrupt `version.json`** and asserts the exit code and the stderr string — never that the baseline survived. The data was one assertion away the whole time. No integration scenario plants a corrupt `version.json` before a destructive command. | **FIXED 2026-08-19.** `preflightVersionStamp` reads the stamp at the top of every committed-artifact writer, inside the same lock the write happens under, so a corrupt one refuses before anything is written — the `build.ts` precedent applied where it was missing. Pinned by 3 tests asserting the DATA rather than the exit code, including the calibration that an ABSENT `version.json` (the ordinary state for a repo that only runs `check`) still writes normally. Mutation-checked. | measured |
| D033 | 2026-08-19 | S3 | **Two independent bug-hunt lens passes, and in one of them by a negative-claim check contradicting itself** — the agent grepped for a symbol, got nothing, then found it via `git grep`'s "Binary file … matches" line. That is the only reason it surfaced | **Three source files contained a raw NUL byte, so `grep` skipped them and `git diff` showed no diff.** `core/src/baseline/fingerprint.ts:15`, `core/src/baseline/scan-history.ts:42`, `plugin-typescript/src/tsconfig-resolver.ts:38` each wrote a literal `0x00` inside a string instead of the escape. **No runtime defect at all** — a literal NUL and the escape are the same one-character string, proved hash-for-hash identical before the fix shipped. The damage was entirely to reviewability, and it landed on the worst possible files: `computeContentFingerprint` is the function move-transfer matches on (D010/D015), and `scan-history.ts` is the whole of ADR 029's admissibility logic. Every change to either arrived in review as `Bin … bytes, 0 insertions(+), 0 deletions(-)`. Measured: `grep -rn computeContentFingerprint fingerprint.ts` → nothing; `grep -a -c` → 1. | **[S-13] a defect that disables the instrument that would find it** — new shape, and the reason it deserves one is that the failure is self-concealing rather than merely subtle | **CLAUDE.md rule 3, and it was rule 3 that the defect disarmed.** The rule requires a grep receipt for every claim of absence; those greps silently excluded these three files, so every absence claim ever made about them was unverified and read as verified. `align check` cannot see it (not an architecture violation), no test read bytes, and the two human-facing instruments — reading a diff and grepping — were the two it broke. | **FIXED 2026-08-19, and promoted in the same change.** The three literals became escapes; `pnpm test`, `align check` and a before/after hash comparison all confirm zero behaviour change. Promotion did not wait for a second instance because there were already three and the invariant costs fifteen lines: `core/test/sources-are-text-not-binary.test.ts` walks every tracked source file and rejects any control byte outside tab/newline/CR, with a calibration case built from `String.fromCharCode` so the test cannot contain the byte it forbids. Mutation-checked — reintroducing one NUL fails it with file, line and `byte 0x00`. **Every grep-based absence claim made in this repository before today should be treated as unverified for these three files.** **Extended to users' repositories the same day**: `align doctor` gained a `binary-source-file` advisory over the files it already scanned — doctor and not a rule, because a control byte is not an architecture violation and a `custom.host` predicate would not survive `check --untrusted`. It names file, line and byte and says what to do; pinned by 4 tests including the tab/newline/CR calibration that keeps it quiet on every CRLF checkout. | measured |
| D032 | 2026-08-19 | S2 | **Trying to wire the consumers an accepted ADR licenses.** The instrument is now four-for-four on this ADR: every gap in it (D021, D023, D030, and both halves of this row) surfaced when someone sat down to implement a section, and none in review | **Two of ADR 029 §6's three unwired consumers were not implementable as specified, and one of them would have broken `align baseline prune`.** **(a) Prune retention.** §6: *"`wasFileObserved` returning `known: true, value: false` for a file the baseline names is a reason to retain"*. That answer means "the previous scan did not observe this file" — which is true of every file deleted BEFORE that scan. Retaining on it makes any entry whose file vanished more than one run ago permanently unprunable, and users prune long after deleting. It reads as a safe addition and is a `prune` that cannot do its job, arriving as a safety feature. Raising N does not fix it: any finite history has the same edge one run further back. **(b) Scope direction.** §6: *"report the narrowing direction only"*. `scopeIdentity` is a HASH of the sorted excludes; two hashes give same-or-different and never narrower-or-wider. Neither shipped, so the user-facing severity is nil — but §6 stood as an accepted licence for both for a day, and the ADR is what a future implementer would have followed. **The cost that had accrued in the meantime**: `wasFileObserved`'s data — the per-domain observed-path lists — was **73% of the record, measured** on align's own repository (24,848 → 6,644 bytes), written into someone else's checkout on every run that changes anything, for a question nothing could soundly ask. | [S-02] a predicate that is unreachable in practice — with a twist worth recording: it was not unreachable by accident but **unreachable by soundness**, and its cost was paid in bytes in every repository rather than in dead code | **Nothing, and nothing reasonably could.** An ADR section is prose; `align check` reads code, and the code implemented §6 faithfully. What is checkable in hindsight is the FIELD: a record field whose only reader is one probe method, and that method having no caller, is mechanically detectable — and would have flagged this on the day `applyMoves` shipped as the only consumer. | **FIXED 2026-08-19.** `wasFileObserved` and the observed-path lists are deleted; §6's prune bullet is struck with the reasoning, and its scope bullet corrected to a direction-free note. The two consumers that ARE sound shipped in the same change: `observedMatchCount` turns "component api matched no files" into "matched 12 file(s) on the previous scan and 0 now" (D011's actionable form), and `previousScopeIdentity` adds "the scan scope changed since the previous run" — both on ADR 023's tier-2 refusal, whose REFUSAL is untouched exactly as §6 requires. Pinned by 8 tests including the three silence cases (no history, count already zero, scope unchanged), because a message that always fires is noise rather than a diagnosis. | measured |
| D031 | 2026-08-19 | S3 | **Asking "what did today's work change the stakes of?"** — not a review of new code, but of code the new code gave a new job. Nothing was edited in `version.json`'s writer; D028 changed what depended on it | **`.align/version.json` had ADR 030's atomic write and neither its lock nor its token**, so its read-modify-write raced. Defensible until this morning: `baselineReconciledBy` had zero readers, and a field nothing consults cannot lose anything that matters. **D028 made it the field `align upgrade` gates on**, and the write discipline was not re-derived at that moment. The lost update, concretely: A runs `recordBaselineReconciled`, setting both fields; B is inside `stampAlignVersion` having read BEFORE A wrote, so B's `{ ...current }` carries no `baselineReconciledBy` and its write erases A's. The user is then offered a reconciliation they just completed — the conservative direction, hence S3, but a lost update of the field deciding whether `upgrade` runs at all. Both halves of the race are ordinary: an MCP server and a terminal is ADR 030's own stated usage. | [S-12] a discipline transplanted from the artifact that earned it — **fourth instance, and the first where the transplant FAILED TO HAPPEN**. The previous three applied a rule where it did not belong; this one withheld a rule from an artifact that grew into needing it. Same root cause read from the other side: the rule lives next to the artifact that earned it and does not travel | **Nothing, and no instrument could.** Every check here reads the code as it is; this defect is a relationship between two commits — one that raised a field's importance and one that set its write discipline months earlier. `align check` cannot see it, no test regressed, and the concurrency suite tests `baseline.json`'s writer, not this one. The instrument that found it was a question, asked after the fact: *what did today's changes make load-bearing that was not?* | **FIXED 2026-08-19.** Both read-modify-writes run under the same lock as `baseline.json`. **A lock and no token, deliberately**: the token exists on `writeBaseline` because its read happens in the caller, seconds and a full scan earlier, so no lock can span it and the write must detect that the world moved; here the read and write are adjacent statements inside one callback, so holding the lock across both closes the window and a token would guard a race that cannot occur [S-06]. **The change created one hazard and closed it in the same breath**: `writeBaseline` holds the lock and calls `stampAlignVersion`, which now wants it — align would have waited 10s for a lock it was holding and reported a CONCURRENT ALIGN, a self-inflicted failure on the most ordinary write in the product. `withAlignDirLock` is now re-entrant per process, sound because the lock excludes PROCESSES and everything under it is synchronous. Pinned by 4 tests, mutation-checked in BOTH directions separately: removing re-entrancy reproduces the self-deadlock, removing the lock reproduces the race. | measured |
| D030 | 2026-08-19 | **S0** | **Investigating an unrelated question.** Asked to characterise the branch-switch coupling (committed `baseline.json` vs gitignored `last-scan.json`), the probe measured a stale record producing one red cycle and then self-healing — benign. Running the SAME probe shape against the D015 fixture showed the identical mechanism is not benign there at all. Nobody was looking for this | **The D015 refusal survives exactly ONE check. It delays the forged transfer by one run; it does not prevent it.** Measured on the real command, same fixture as `d015-move-forgery.test.ts`: `check#1` exit 1, consent held at `src/api/old.ts`; `check#2` **exit 0**, baseline `['src/api/new.ts']` — a human's `acceptedBy: manual` on a violation nobody reviewed; `check#3` green. **Mechanism**: the refusal requires the previous scan to have observed the orphan violating at its own path (the two-sided coexistence rule added hours earlier for D024). `check#1` refuses AND rewrites the record from the current tree — where the orphan's file is deleted and therefore not observed. So `check#2` finds candidate-only evidence, which is precisely the state D024 says means "the record advanced past a lost baseline write, re-derive the transfer". The two fixes collide: D015's evidence is destroyed by the writer one run after it is first used, and the resulting state is indistinguishable from D024's. In CI, where every push runs check, the delay is minutes; on a fresh clone there is no record at all and the delay is zero. | [S-09] fixed one arm, missed the other — the arms being two defects whose fixes are individually correct and jointly unsound. Also a new sub-case worth naming: **evidence with a shorter lifetime than the condition it justifies.** The record is a snapshot rewritten every run; coexistence is a fact about the past that stops being representable the moment one participant is gone | **My own integration scenario, which stops exactly one step short.** `scan-history-refuses-forged-transfer` runs three `check`s and only ONE after the deletion — so it observes the refusal and returns before the run that undoes it. Every unit test does the same. The calibration discipline this project uses (prove the fixture reproduces the defect) was applied and passed; what was never asked is whether the fix HOLDS, which needs a second invocation. No instrument here runs a command twice and asks whether the answer is stable. | **FIXED 2026-08-19** with candidate (1), sticky evidence — and implemented without the plumbing that option described. Surfacing refusals through `CheckRun` proved unnecessary: the writer already holds the previous record and can be handed the post-transfer baseline, which is enough to compute retention from data rather than from a decision. The record gains `retained`, a SEPARATE list beside `violations` — merging them would have made the record claim to have observed a file it never saw — and `wasViolationObservedAt` consults the union. Retention is conditional on a current baseline entry still naming that exact violation at that exact path, so it converges on human action rather than on a timer, and the bound is the baseline itself. **The claim "closes D015" in commit a41d4ed was wrong and is corrected here rather than in rewritten history.** Pinned by 4 CLI tests (three consecutive refusing checks, not two — the failure mode is surviving exactly one; plus convergence after `prune`) and by two new steps on `scan-history-refuses-forged-transfer`, the scenario that stopped one step short. Red-before-green measured at both levels: without retention, step 9 passes and step 10 exits 0 with the consent re-homed. **The cost is recorded, not hidden**: a stale record now refuses persistently instead of self-healing, which shows up after a branch switch (committed baseline, gitignored record) and is pinned by its own tests — deliberate, because align cannot distinguish that case from the forgery. | measured |
| D029 | 2026-08-19 | S2 | The same research agent, enumerating which `.align/` artifacts travel through git and which must not — a question nobody had asked as a LIST | **`.align/.lock` was gitignored nowhere, and a foreign-host holder was unbreakable at any age.** Two halves, each individually defensible, together a repository-wide outage. (a) `.lock` (ADR 030) is created and deleted inside a single command, so a copy in git is by definition a copy of a lock somebody lost — yet it was absent from `.gitignore` and from `ALIGN_LOCAL_GITIGNORE_ENTRIES`, whose comment listed "baseline.json, generated-rules.json, ruleset-ir.json" as the committed set: three of six, a sample presented as a census, which is how `.lock` and `version.json` both went unlisted. (b) `isBreakable` returned false for any holder whose `host` differed from this machine's, at any age, because a foreign pid cannot be liveness-checked. So one `git add -A` after a SIGKILL committed a lock that blocked every WRITING align command on every teammate's machine and every CI run, permanently. **Measured**: a planted two-year-old lock from `buildbox-01` made `align baseline accept` wait the full 10s timeout and exit 1, every time. `align check` still worked, and the error message names the file to delete — which is why this is S2 and not higher. | [S-12] a discipline transplanted from the artifact that earned it — **third instance, and the first found INSIDE the function whose own comment refutes it**: the unidentifiable-holder branch two lines below says in as many words that for a file align creates, owns and deletes, never-break is the UNSAFE direction, and cites the bricked repository ADR 030 §4 was amended for | **Nothing could see it.** `.lock` was absent from the harness's `ALIGN_DIR_FILES`, so no capture, no snapshot and no write-set had ever observed the one `.align/` file whose presence after a command is always a defect. The unit suite had 16 lock tests and one of them, `never breaks a lock held by another HOST, however old`, asserted the defect as a guarantee. | **FIXED 2026-08-19.** (a) `.align/.lock` added to `ALIGN_LOCAL_GITIGNORE_ENTRIES` (so `init` writes it into every repository) and to align's own `.gitignore`; the false census comment replaced with the real six-item list. (b) A foreign holder is breakable at `FOREIGN_HOST_STALE_AFTER_MS` — ten minutes, ten times the local floor and three orders of magnitude above what ADR 030 actually holds the lock for (a token compare, a write, a rename; never the scan), so it is a bound past which "another machine is mid-commit" has stopped being a possible explanation. The break message differs by branch, because on this host align has CHECKED the process is gone and remotely it has only waited. The test asserting the old guarantee is replaced by three: breaks when old, still WAITS when young, and the local floor cannot shorten the foreign one. Mutation-checked. Plus `stale-foreign-lock-clears`, and `.lock` added to the harness's captured set — which makes "no command leaves a lock behind" a property every scenario now asserts for free. | measured |
| D028 | 2026-08-19 | S1 | **A research agent testing a hypothesis that turned out to be false.** The brief was "most align defects are cross-invocation" — measured across 31 rows it is 19%, the SMALLEST class. The two defects the agent found while disproving it were both cross-invocation, which is the actual result: the class is under-hunted rather than dominant | **`align upgrade` gated on the wrong field of `.align/version.json`, and the right field had zero readers.** `alignVersion` answers "who last wrote anything under `.align/`" and is stamped by every committed-artifact writer — `baseline accept`, `baseline prune`, `build --apply`, `export-ir`, and any `check` that move-transfers. `baselineReconciledBy` answers "the version under which the baseline was last DELIBERATELY reconciled" and is written only by `init` and `upgrade`'s final step. `upgrade.ts` read the first. So ONE unrelated command under the new binary — `align export-ir`, which does not touch `baseline.json` at all — made every later `align upgrade` print `Already at the current version — nothing to reconcile` and exit 0, permanently, with no way back except a `--from` flag the user has no signal to reach for. **`upgrade` also disarmed itself**: a run blocked by ADR 023 tier 2 still performs the accept half, which stamps — so the very run printing *"Re-run `align upgrade` to finish"* is the run that makes the re-run a no-op. Measured end-to-end on real binaries. | [S-09] fixed one arm, missed the other — the sharpest variant yet: **a guard protecting a field nothing reads.** Also [S-03]: `runUpgrade`'s doc comment asserts "any failure before the final call leaves `baselineReconciledBy` exactly as it was, so a re-run sees the same unreconciled state and tries again" — the first half is true and the second is false, because the re-run never looks at that field | **Nothing, and the project had already written the refutation.** `version-file.ts`'s own doc comment for the field records that an earlier ADR draft specified last-writer-of-`baseline.json` and was rejected because incidental writers "would make a 'last writer' field read as current after routine CI, **defeating the field's purpose**". The hazard was identified, a field was built to avoid it, and the consumer was then wired to the hazard — with the sentence explaining why that is wrong sitting in the same package. No test could have caught it: every `upgrade` test stamped the two fields together or stamped neither, so the two questions were never asked of different answers. | **FIXED 2026-08-19.** `upgrade` reads `baselineReconciledBy`; the fallback is `'unknown'` and **never** `alignVersion` — substituting it is the defect, not a milder form of it, because it asserts a reconciliation that may never have happened. `init` writes the watermark unconditionally, so the only repositories without one are those stamped by a bare writer or by an align predating ADR 022, and offering to reconcile those is the conservative direction. Pinned by 2 unit tests (mutation-checked: both fail on the old expression) and by `upgrade-survives-an-unrelated-stamp`, a new scenario where `export-ir` is the load-bearing step — red-before-green measured end-to-end, failing at exactly the pin while its calibration step still passed. | measured |
| D027 | 2026-08-19 | S3 | **The act of writing a test** — for a resolver that had just been extracted, which is the whole reason the extraction was worth doing: the branch had existed since 0.1.x and had never been executable without mutating `process.env` | `ALIGN_AGENT_MODEL=` (set but empty — how a variable gets "unset" in a shell profile or a CI matrix row) produced an EMPTY model id rather than falling back to the default. `??` treats only `null`/`undefined` as absent, so `options.model ?? process.env['ALIGN_AGENT_MODEL'] ?? DEFAULT_MODEL` returned `''`, which travelled to the Anthropic SDK and failed there — an error about the model where the cause was the configuration. Pre-existing and shipped; surfaced while moving the read out of `AnthropicFixProvider`'s constructor to the CLI entry point, not by that move. | [S-02] a predicate that is unreachable in practice, inverted — a FALLBACK that is unreachable for one of the two ways users express "no value" | **Nothing, and the shape of the gap is the point.** The branch had no test because testing it meant mutating `process.env` inside a worker process shared with 683 other tests. An ambient read is not merely a style violation; it is a branch that costs more to test than it is worth, so it does not get tested. `--model` was covered; the env half never was. | **FIXED 2026-08-19** alongside the extraction. `resolveAgentModel` trims and treats empty as unset. Pinned by 4 tests including the empty case, which is now a pure call with no process state involved. | measured |
| D026 | 2026-08-18 | S2 | **A generic CLI best-practices checklist run against a mature CLI** — a new instrument for this project, and the point of the row: it found something eighteen months of domain-specific review, 1445 tests and 23 integration scenarios did not, because every one of those asks whether align's ANSWERS are right and none asks whether align is a well-behaved UNIX process | **No `EPIPE` handler anywhere in the CLI.** When a downstream reader closes the pipe before draining stdout, Node raises an unhandled `'error'` event on the stdout stream and align dies with a raw stack trace (`node:events:487 throw er; // Unhandled 'error' event` / `Error: write EPIPE`) instead of exiting quietly. **Measured 2026-08-18**: `align skill --topic authoring \| (exec true)` prints 1188 bytes of Node internals to stderr. **Inferred, not measured**: the same crash on the ordinary `align <cmd> \| head -N` form once output exceeds the pipe buffer (~64KB on this machine) — every payload this repository can currently produce is smaller (`check --json` 4022 B, `skill --topic authoring` 13360 B, `docs` 1369 B), so `head` drains them fully and the crash does not fire here. A repository with hundreds of violations reaches it through `check --json`, which is align's stated target (n8n: 207 real cycles on first evaluation, ADR 006's evidence section). Related, same instrument, smaller: usage errors exit **1** where the convention is **2** (`align check --nonsuch` → 1, commander's default, never overridden), which matters more than a convention normally would here — **exit 1 is also `align check`'s RED verdict**, so a typo'd flag in CI is indistinguishable from a repository that genuinely has violations. ~~There is no `SIGINT` handler, so Ctrl+C exits 0/1 rather than 130.~~ **That third claim was false and is withdrawn** (measured 2026-08-18, at the fix): Node with no SIGINT listener restores the signal's default disposition and the shell already sees **130** — `node -e "setTimeout(()=>{},10000)"` interrupted gives exit 130. align satisfies that rule by doing nothing, and ADDING a handler is what would break it: a listener that sets `process.exitCode` without calling `process.exit` suppresses the default kill and makes Ctrl+C stop interrupting align at all. The checklist item was read as a gap without measuring the runtime's existing behaviour — the same failure the ledger keeps recording about doc comments, committed here against a checklist instead. | [S-03]-adjacent but genuinely new: **a whole class of correctness nobody owns.** Not a wrong answer, a wrong PROCESS contract. Worth its own shape id if a second instance appears; recorded here as a single instance rather than promoted on one. | **Nothing, and nothing was pointed this way.** `align check` on this repo checks align's architecture; the write-set invariant checks what commands write to disk; ADR 023's tiers check what they delete; the 23 integration scenarios drive real binaries but always read stdout to completion, never as a closing pipe. The gap is structural: every instrument this project owns was built to ask whether align's ANSWERS are right. | **FIXED 2026-08-18**, in `packages/cli/src/process-contract.ts` — a module named for the thing that had no owner. (a) An `'error'` listener on stdout and stderr swallows `EPIPE` and nothing else; a real write failure (ENOSPC under `align check > out.json`) is still reported and still sets exit 1, because an error handler that silences everything would be this project's most-repeated shape wearing a fix. It deliberately does NOT `process.exit(0)`, the conventional EPIPE response: align's exit code carries a VERDICT, so a quiet 0 on a broken pipe would report success for a red repository. Safe because every persistence step in `runCheck` completes before the first byte of output is written — checked, and the comment says what to do if that ordering is ever inverted. (b) `program.exitOverride()` plus a mapping in `index.ts`: usage errors 2, `--help`/`--version` 0, non-commander errors rethrown unchanged. (c) The SIGINT half was withdrawn rather than implemented, and the empty implementation is documented in place so nobody 'fixes' it later. Measured before/after against the built binary: EPIPE 1188 bytes of Node internals → 0; `check --nonsuch` 1 → 2. Pinned by 10 unit tests (mutation-checked: 9 of 10 fail with either half reverted) and by `usage-error-is-not-a-red-verdict`, a new integration scenario calibrated `expectFailOn: ['0.1.4']` — measured there, failing at exactly step 1 for exactly the right reason. Its write-set is the install's two files, which makes "a rejected command line writes nothing" an assertion too. **Not automated: the end-to-end EPIPE path**, because align's own ruleset forbids `node:child_process` outside the git rails, so no test here may spawn the binary — hence the injected streams, and hence that measurement being by hand. | measured |
| D025 | 2026-08-18 | S2 | Adversarial review, reading the newly-added guard and asking the completeness question of it — "§7.5 stops an ERRORED run writing; what else makes a run know less than the repository contains?" | `persistScanObservation` refused to write from an errored run and wrote freely from a merely INCOMPLETE one. An incomplete run reports fewer violations by construction — an unresolved specifier drops the edge its violation would have fired on, an ungrounded component evaluates its rules over nothing — so persisting it replaced a sound record with a narrower one. The next run then answered `known: true, value: false` about a violation the previous COMPLETE scan had seen, and D015's brand-new refusal did not fire. One `align check` without dependencies installed disarmed it, silently and with no way for a user to notice. Never unsound (fewer refusals is pre-ADR-029 behaviour exactly, so no false green) — the defect is that the protection became invisible and variable. **The first fix proposed for it was wrong, and for a stated reason that was false**: the residual was left open on the grounds that refusing to write when incomplete would inert the mechanism on any repository with a standing `uncertainty` advisory, align's own included. `isRunComplete` does not consider `uncertainty` at all, and align's own run is complete (measured). | [S-09] fixed one arm, missed the other — **seventh instance**, and this one inside a guard added hours earlier for the other arm | **Nothing, and the guard itself is the near miss.** §7.5 was written from a reproduction of the errored case and pinned by a test that names the reasoning verbatim; the identical argument applies to incompleteness and neither the test nor the ADR asked. ADR 023 has separated these two axes since 2026-08-08 — the vocabulary for the question existed, in a document this ADR cites. | **FIXED 2026-08-18.** The record carries `complete` (`isRunComplete`, required field — pre-release shape churn is why it need not be optional), and the writer declines to replace a complete record with an incomplete one, announcing it on stderr. A no-DOWNGRADE rule rather than no-write-when-incomplete, forced by measurement: the integration project is `complete: false` at its pinned commit and every unit fixture is incomplete for lack of `node_modules`, so the naive rule would never write a record on the repositories that matter most — measured when three of the first-draft tests failed on that assumption. `complete` is write-eligibility, never admissibility: an incomplete scan still OBSERVED what it reported, and what it gets wrong is what it omits, which is invisible in the record by construction. Pinned by 4 tests over the four transitions, mutation-checked (removing the guard fails exactly the downgrade one), plus a sixth corruption-table case for the new required field. | measured |
| D024 | 2026-08-18 | S1 | **Two independent adversarial reviews of the same uncommitted change, run in parallel on different models and briefed identically** — the comparison is itself the finding: their only overlap was the per-candidate steering, they each found a class the other missed entirely, and the severest defect was found by exactly one of them | **(a) S1, the substantive one.** The D015 refusal asked only whether the CANDIDATE was observed last scan, treating "observed at some point" as "observed BEFORE the orphan vanished". Those come apart whenever the record advances past the baseline: a run that transfers records the moved violation at its NEW path, so if that baseline write is lost or reverted, the next run finds its own predecessor's record and refuses a rename it had already made — **permanently**, since every later run re-records the same observation, and `baseline prune` then deletes the orphan outright. `commands/check.ts` tolerates a failed `persistMovedBaseline` on the explicit stated ground that *"the next `align check` re-derives and re-persists the same transfer unconditionally"*, and `commands/baseline.ts` defers transfers relying on the same invariant — the change broke both while leaving both comments in place. Reachable three ordinary ways: the tolerated concurrent write, `git checkout -- .align/baseline.json` after align auto-wrote a transfer, and a branch switch (baseline is committed, the record is gitignored). Reproduced end-to-end. Direction is red-not-green, so S1 rather than S0. **(b) S3, a family of five doc defects in text hours old**, each a comment asserting something about code it does not govern: the safety sentence *"nothing this predicate can say makes a transfer happen that would not have happened before"* (false — matching is greedy WITH consumption, so declaring one candidate already-observed shifts later orphans onto different targets; measured on 2 orphans and 3 identical candidates); a forged-record threat model that enumerated what an attacker cannot do while omitting the deletion they can cause via `prune`; a writer census reading "three" when `align_violations` shares `freshCheck` and made it four — with **neither MCP writer covered by any test**, under a doc comment arguing at length that MCP must be a writer or the mechanism is "permanently inert for align's stated primary consumer"; two comments contradicting each other about which surfaces consult the probe; and ADR 029 §7.3's amended "if and only if" contradicted by its own next sentence. | [S-03] a comment asserting a fact about other code, unverified — for (b); **[S-09] fixed one arm, missed the other** for (a), where the arm is one half of a two-sided temporal claim | **Nothing, and the author's own stance-switch pass is the near miss.** CLAUDE.md §1.8 was run before these landed and caught a different overclaim (the `prune` transfer→deletion conversion) while missing all six of these. That is §1.8's own thesis measured: the author checks the parts they thought about. Neither `align check`, the write-sets, nor 1440 unit tests can see a false sentence in a comment, and (a) had no failing test because every fixture advanced the record and the baseline together. | **FIXED 2026-08-18, before commit.** (a) `applyMoves` now requires BOTH sides — the previous scan must have observed the orphan violating at its own recorded path AND the candidate at its new one, which is what "they coexisted" actually means and is what the D015 recipe produces. Reproduced red first (`cli/test/d015-move-forgery.test.ts`, "a record that advanced past a lost baseline write must not strand the rename"), then green. (b) All five corrected in place with the measurement that refutes each, plus 2 new tests covering both MCP writers (mutation-checked: removing the call fails both). | measured |
| D023 | 2026-08-18 | **S0** | The act of implementing an accepted ADR's consumer list — writing the code §6 licenses forced the question "what fact authorizes this?", and the ADR had no answer that its own §5 permits | **ADR 029 §6 licenses `store.applyMoves` to retire ADR 006's whole-directory rename exception, and ADR 029 §5 forbids exactly that.** §5: *history is admissible as a refusal, never as a permission.* §6, sixteen lines later: the refusal "becomes violation-precise instead of directory-granular, so an ordinary whole-directory rename transfers again" — i.e. a transfer that is refused today becomes allowed. The only fact available for it is `wasViolationObservedAt(candidate) === false`, an ABSENCE, which cannot separate "the candidate did not exist last scan" (rename) from "last scan never looked there" (partial checkout, `excludes` match, unreadable directory, symlink, nested checkout). `wasFileObserved` cannot substitute: for a genuine rename the candidate's file is *new*, so a positive observation of it is precisely what a rename does not produce. **Never implemented** — the refusal half shipped, the retirement half did not. Had it been, the regression would have landed in the defect's own habitat: the record is gitignored and machine-local (§1), so a fresh CI checkout has none, the new refusal cannot fire, and D010's forged transfer at exit 0 returns unguarded. The pinning test's comment and both ADRs had already been edited to instruct the revert, so the wrong half was the half a future reader was being told to do. | [S-10] absence treated as evidence — **third instance**, and the first found in a DESIGN document rather than in code | **Nothing, and no existing check could.** Every instrument this project has reads code: `align check`, write-sets, the ADR-023 guards, the exhaustiveness arms. A contradiction between §5 and §6 of one markdown file is invisible to all of them, and both sections were reviewed and accepted. The one instrument that found it was writing the code — which is also how D021 (the previous ADR-029 defect) surfaced. Two design defects in one ADR, both caught only at implementation, is a measurement about ADR review in this project, not about this ADR. | **RETIREMENT STRUCK 2026-08-18; the refusal shipped.** `applyMoves` now asks `wasViolationObservedAt(candidate)` and refuses on `true`, which closes D015 and covers D010 wherever a record exists; ADR 006's whole-directory exception STAYS and covers the case where none does. ADR 029 §6 and ADR 006's "intended to be temporary" section are both amended in place with the reasoning, and `orchestrator.test.ts`'s comment withdraws the revert instruction rather than deleting it. Pinned by 10 core tests + 3 CLI tests running the real command; the D015 reproduction exits **0 green** with the refusal removed and **1 red** with it, measured in that order. | measured |
| D022 | 2026-08-18 | S3 | A test I had just written, failing on the previous run's leftovers — `EEXIST` from `mkdirSync` on a path a *committed fixture* already contained | `mcp.test.ts` started MCP servers against `packages/cli/test/fixtures/simple-app` and `simple-app-violation` **in place**, at 16 call sites. Safe for exactly as long as no MCP tool wrote anything — and ADR 029 made `align_check` a writer, so `pnpm test` began leaving `.align/last-scan.json` inside two committed fixture directories, showing up as untracked files in `git status`. No user-facing data loss: the blast radius is the working tree of whoever runs the suite. The same file also asserts `expect(fs.existsSync(path.join(rootDir, '.align/generated-rules.json'))).toBe(false)` — a "this must not write into the fixture" assertion, for one artifact, written by someone who had noticed the hazard for that artifact and not for the class. | [S-11] a writer trusted to stay inside its own region — **second instance**, one level below where the invariant lives | **Nothing, and ADR 026 is the near miss.** Declared write-sets make exactly this fail-closed at the integration level; unit tests have no equivalent, so the same command writing the same file is caught in Docker and unnoticed in vitest. It surfaced only because the leftovers happened to collide with a new test's setup. | **FIXED 2026-08-18, and promoted.** Every server now runs against `copiedFixture(name)`, a throwaway copy. The promotion is the part that transfers: `connectedClient` itself **refuses** a `rootDir` inside `test/fixtures/`, which is order-independent — a "fixtures stay clean" assertion in some other test file would only catch the pollution if it happened to run last. | measured |
| D021 | 2026-08-18 | S2 | The act of implementing an accepted ADR — §7.4 could not be written as specified without noticing it contradicted the amendment made to ADR 030 the previous day | ADR 029 §7.4 specifies "a corrupt record throws; it never reads as empty" for `.align/last-scan.json`, citing BUG #1. The precedent does not transfer: `baseline.json` holds irreplaceable human consent and an empty read there precedes a full-snapshot overwrite, whereas `last-scan.json` is a gitignored machine-local cache align creates and replaces on its own schedule, and an absent read yields `known: false` from every `ScanHistoryProbe` question — which §5 defines as *exactly today's behaviour*. Implemented as written, a corrupt cache file the user cannot see in `git status` would fail every `align check` in the repository until they deleted it by hand. **Never shipped**: caught between the ADR being accepted and the code being written. **Measured 2026-08-18** by implementing §7.4 as specified and running the integration scenario against real binaries: with a truncated record planted, `align check` leaves it truncated **forever** — every later read throws, the guarded writer swallows it and skips the write, so no warning is ever printed and the whole mechanism is silently dead from that point on. The harsher outcome — the command itself failing — is *inferred, not measured*: today the only read is inside `persistScanObservation`'s catch, but §3's probe is constructed from the record **before** the scan, so once the consumers land (task #14) the throw is uncontained. | [S-12] a discipline transplanted from the artifact that earned it — **second instance in two days**, and the first (ADR 030 §4's unbreakable `.align/.lock`) was measured bricking a repository | **Nothing — and "nothing" is too generous.** The reasoning that refutes §7.4 was already written down in this repository, **twice, before the ADR was drafted**: `readTelemetryState` treats a corrupt state file as empty, and `readBaseline`'s own doc comment states the distinguishing criterion in one line — *"Mirrors `readRulesetIr`'s discipline, not `readTelemetryState`'s — this file holds irreplaceable human consent decisions, not a regenerable cache."* `docs/core-interfaces.md` spells out the same split. ADR 030 §4's amendment, one day earlier, is the third. So the failure is not that the argument was unavailable; it is that a rule was cited **by name** instead of re-derived for the artifact it was being applied to, with the criterion sitting four functions above the new one in the same file. | **REVERSED before implementation.** A record that fails to parse — truncated, empty, wrong shape, unknown `recordVersion` — reads as absent and says so once on stderr; the same `check` replaces it, so the warning does not repeat and no human has to delete anything. ADR 029 §7.4 is amended in place with the reasoning. Pinned by 7 tests driven from a table of six corruption modes, so the property under test is the universal ("no read failure escapes as an exception") rather than six separate cases, plus `scan-history-record-written` end-to-end — red-before-green measured in that order. | measured |
| D020 | 2026-08-18 | S2 | Adversarial review, briefed from `SHAPES.md`, reading code the same day it was written | Two further causes of D016's exact symptom, both in the function D016's own fix had just rewritten. **(a) Double count**: `matched` sums violations under their POST-transfer paths while the retention partition counts baseline entries under their PRE-transfer paths, so a move-transferred entry sits in both — **measured `{previous:1, current:2, delta:+1}`**, align reporting that debt GREW because a file was renamed. The code comment asserting the pre-filter "guarantees no double counting" was false. **(b) Duplicate fingerprints**: `previous` counted ROWS while every other consumer (`InMemoryBaselineStore`, `prune`, the matcher) keys by fingerprint, so a hand-edited or merge-mangled `baseline.json` reported a permanent `-1` on every run. | [S-09] fixed one arm, missed the other — **fifth and sixth instances**, both inside the fix for the third | **D016's own tests, written hours earlier.** They enumerated the cause that had been reproduced and stopped there; neither a move nor a duplicate row appeared in any fixture. `store.applyMoves` incidentally prevents (a) end-to-end by refusing to transfer anything the partition would retain — but that lives in another package, is asserted nowhere, and the two predicate sets read different inputs (one domain's blind spots vs the union). | **FIXED 2026-08-18.** `previous` now counts DISTINCT fingerprints; `current` is clamped to `previous`, since live debt cannot exceed the entries recording it and a violation of that is an impossibility rather than a number to print. The false comment is replaced by one stating what the pre-filter does and does not do. Pinned by 3 tests including a calibration that two rules violated in one file still count as two. | measured |
| D019 | 2026-08-18 | S3 | Adversarial review; then the enforcement test itself, once broadened — it named all five in one run | `docs/core-interfaces.md` had five stale blocks beyond the one repaired that morning: `CheckRun` (missing `blindSpots`, `observedFiles`), `DependencyGraph` and `ManifestInventory` (`blindSpots`), `ScanInput` (`includeNestedCheckouts?`), `BaselineEntry` (`contentFingerprint?`, `acceptedValue?`). `ARCHITECTURE.md` §3 cites `DependencyGraph.blindSpots` by name, so a reader following that citation into the reference found no such field. Also in the family: `packages/agent/README.md` claimed every unscanned path is reported as a `scan-blind-spot` advisory — two reasons deliberately emit none and nested checkouts use a different `kind`, which matters because an agent filters on it. | [S-03] comment asserting a fact about other code, unverified | **The enforcement test added that morning.** It checked exactly one interface while its describe read "stays in step with the interfaces it documents" — a green that a reader would take as covering the document. The overclaim was the defect; the drift was its consequence. | **FIXED 2026-08-18.** The test now iterates an `ENFORCED` list of six interfaces with their source files, so drift reports itself by name; the five blocks were brought to reality from what it printed rather than by hand. The agent README now names the exceptions and points at `blindSpots` instead of the advisory kind. | measured |
| D018 | 2026-08-18 | **S0** | Adversarial review during ADR 028 Stage 1, which recorded it in that plan's out-of-scope list rather than fixing it — the ledger row is being written now, at the fix, which is later than this project's own rule wants | Two ways `.align/baseline.json` could be destroyed with no command deciding to destroy it. **(a) Torn write**: every `.align/` writer used `fs.writeFileSync`, which truncates before it writes, so a process killed mid-write leaves a truncated file. `readBaseline` then correctly refuses to treat corrupt as empty and throws — and the obvious recovery, deleting the file and re-running `baseline accept`, launders every unreviewed violation in the repo into the baseline under the user's name. **(b) Lost update**: `writeBaseline` is a full-snapshot REPLACE wrapped in a read-modify-write whose middle is a multi-second scan, with no check that the file still looked as the caller expected. Two overlapping aligns — the shipped MCP server and a human's `baseline accept` is the intended usage, not a corner case — and the second write erases the first, both reporting success at exit 0. | [S-10] absence treated as evidence, inverted: *presence* assumed unchanged | **Nothing, and nothing could have.** No test simulates two processes; the write-set invariant (ADR 026) checks what one command wrote, not what a concurrent one lost; ADR 023's tiers gate what a command decides to delete, not what it silently overwrites. This is the first defect here whose whole shape is outside the single-process model every existing check assumes. | **FIXED 2026-08-18, [ADR 030](../030-2026-08-18-align-dir-write-integrity.md).** (a) `writeFileAtomic` — same-directory temp, fsync, rename, best-effort directory fsync — on all seven full-snapshot writers. (b) A short lock (`.align/.lock`, `open(…,'wx')`) held only around the commit, plus a content-hash token taken at read and compared INSIDE the lock; mismatch refuses loudly and writes nothing. Neither half works alone: the lock does not span the scan, so it cannot see the staleness; the token compare outside a lock would be its own race. Token parameter required, no opt-out — the compiler enumerated all seven call sites ([S-09]'s executable technique, used twice in one day). Pinned by 21 tests. **Two defects in the fix were caught before shipping**: an import cycle, by `align check` on this repo, and an `EEXIST` from the lock body being mistaken for lock contention, by writing the test. | reconstructed |
| D017 | 2026-08-18 | S1 | A scratch probe written to close a known plan item — and the instrument is the point: the plan named **two** exits in this domain, the probe found **four**, and one of the two it named was already dead code | The manifest domain's absence exits ABOVE the per-manifest read. ADR 028 F3 correctly fixed `buildManifestRecord` and `loadWorkspacePackages` to read-then-branch-on-`code` — but `loadWorkspacePackages` opens with `if (patterns.length === 0) return []`, and the layer producing those patterns failed silently. **Measured against the built scanner**, two-package workspace, one break each: a malformed `pnpm-workspace.yaml`, an unreadable one, an unreadable glob directory — all three scanned the root manifest ONLY and returned `blindSpots: []`. A malformed `pnpm-lock.yaml` returned `lockfilePresent: false`, indistinguishable from no lockfile, which also silently changes results (every `catalog:` specifier falls back to raw package.json text, so `security.manifest.source-hygiene` evaluates a string that is not what ships). No data loss — ADR 028 mechanism 2 retains the entries — but align looked at a fraction of the repo and reported green. | [S-09] fixed one arm, missed the other — **fourth instance** | **ADR 028 F3's own tests.** `manifest.test.ts`'s F3 block tested the per-manifest read thoroughly and stopped exactly where the read stopped; nothing exercised the layer that decides whether that read happens. `align doctor`, whose stated job is a read-only survey of scan blind spots, also reported nothing. | **FIXED 2026-08-18.** The blind-spot sink now threads through `readWorkspaceGlobs`, the glob walk (`safeReaddir`) and `readLockfile`, each telling ENOENT (sound, silent) apart from unreadable/unparseable (recorded). Config-file records REPORT only — a member set align could not read has no enumerable paths to bind containment to, and recording the repo root to force it would retain every entry in the repo over one malformed YAML file ([S-04]); retention there is mechanism 2's job, which already covers it. The unreadable-DIRECTORY record does carry a containing path, because that subtree is known exactly. Dead `readJson` deleted. Second half of the item closed too: the domain's private exclude dialect is gone, replaced by an import of the source walker's own `matchingExcludePatternForDirectory`, so `excludes: ['packages/*']` can no longer hide a package's sources while keeping its manifest (BUG #4's precedent — a second glob implementation that diverged three ways). Pinned by 6 tests, red-before-green measured for all; end-to-end confirmation that `align check` now prints the advisory. | measured |
| D016 | 2026-08-18 | S1 | A hand probe while writing ADR 028 Stage 5's retention scenarios — the scenario itself passes, and would have gone on passing | `computeBaselineDebt` (`cli/src/commands/check.ts`) sums each gate's `baselinedCount` for the CURRENT scan and compares it to the on-disk baseline's length. When entries are unobservable, no current violation matches them, so the sum is 0 and align reports a debt drop that never happened. **Measured 2026-08-18** on a two-entry world hidden behind one `excludes` pattern, against the built binary: `baselined debt: 2 → 0 (-2)`, `verdict: green`, **exit 0**, with both accepted entries still in `.align/baseline.json` and nothing fixed. `prune` on the same state correctly prints `Retained 2 entries`, so the two outputs of the same release contradict each other. Reaches `align check`, `align upgrade`, `--json` and MCP alike. | [S-09] fixed one arm, missed the other | **The guard inside this very function.** It already refuses to report a drop on an errored run, with a comment naming the exact hazard — "summing on an error run fabricates a debt DROP (`47 → 0 (−47)`) exactly when nothing was verified — a false 'debt eliminated' ratchet signal". ADR 028 then added a second cause of exactly that fabrication and the guard was never extended to it. The reasoning was already written down; only the new arm was missed. | **FIXED 2026-08-18.** An unobservable entry is now counted as STILL BASELINED rather than the line being suppressed — suppression was the obvious move and is [S-04]: with 500 entries, 2 hidden and 10 genuinely fixed, "report no change" would hide a real paydown to avoid a 2-entry error. Reuses `partitionBlindSpotCandidates`, the same partition `prune` uses, so the reporting path and the destructive path cannot disagree again — the disagreement WAS the defect. The probe is a **required** parameter, which is [S-09]'s executable sub-case: the compiler enumerated all four consumers (`check` ×2, `upgrade`, MCP) and an optional one would have left MCP behind. Pinned at two levels, red-before-green measured in that order at both: 5 unit tests in `baseline-debt.test.ts` (including the anti-[S-04] mixed-run case and a calibration test that a real deletion still reads as a drop), and `blind-spot-excludes-shrink-retains` step 9, whose own output went `baselined debt: 2 → 0 (-2)` → `2 → 2 (0)`. `builder.ts`'s `fallbackBaselineDebt` was checked and is NOT a third instance: with no previous baseline it sets `previous = current` and can never report a drop. | measured |
| D015 | 2026-08-18 | **S0** | Design agent, while designing the scan-history record | The forged transfer has a **file-level** sibling that D010's directory-granular fix does not cover, and it needs no partial checkout. Baseline `src/api/old.ts`; add a content-identical, never-reviewed violation at `src/api/new.ts` (red); delete `old.ts`. `applyMoves` sees old-gone plus a content match and transfers the human's `acceptedBy` onto the unreviewed violation — `align check` goes red → **green** at exit 0. Reproduced 2026-08-18. Pre-existing: this is FRAGILE #7's content-fingerprint identity working exactly as designed, and no snapshot can separate "moved here" from "deleted, and an identical thing was already there". | [S-10] absence treated as evidence | Nothing. ADR 006 *requires* transfers, mechanism 3 is directory-granular, and the candidate is a legitimate current violation. Every guard is behaving correctly. | **FIXED 2026-08-18** (was: open — mitigated, not fixed). No sound fix exists without a temporal reference: the sound test is "was this candidate already RED last scan" — if it was, it existed before and cannot be the move target. That is `wasViolationObservedAt` in [ADR 029](../029-2026-08-18-scan-observation-history.md) §6, which supersedes the scan-history proposal and closes this row soundly. **Shipped 2026-08-18 (mitigation)**: `describeMovedEntries` turns the bare count into a reviewable report naming both paths, the rule and the acceptor, on `align check` AND `prune`, and states the limit of align's knowledge so a reader knows the report can be wrong. Detection only — the forgery still happened and still exited 0. **Closed 2026-08-18 (fix)**: ADR 029's record landed, and `applyMoves` now asks `wasViolationObservedAt(candidate)` before transferring — `true` means the candidate was already violating at that path last scan, so it predates the orphan's disappearance and cannot be where the violation moved to. Refusal only, per ADR 029 §5: `known: false` leaves every decision as it was, so a repository with no record behaves exactly as before. Measured against the real command (`packages/cli/test/d015-move-forgery.test.ts`): with the refusal removed the reproduction exits **0 green** with `acceptedBy: manual` on the never-reviewed violation; with it, **1 red** and the acceptance stays where the human put it. **Residual, stated rather than closed**: a first run has no record, so D015 is still reachable on a fresh checkout — the mechanism needs two scans by construction. Pinned by 10 core tests + 3 CLI tests, including a calibration that a genuine rename in the same window still transfers. | measured |
| D014 | 2026-08-17 | S3 | Adversarial review (both) | Comments citing `degenerate-scan.ts` and a mutation named `use-harness-floor-component`, neither of which exists; a test header citing a deleted test file; orphaned `dist/degenerate-scan.*` from the removed module. | [S-03] comment asserting a fact about other code, unverified | Nothing — none of it is executed. | Corrected in place; stale `dist/` artifacts deleted. | measured |
| D013 | 2026-08-17 | S2 | Adversarial review (Opus) | The consent gate is a **breaking CLI change** with no documentation: README's CI recipe and agent cheat-sheet still show bare `align baseline prune`, `-y/--yes` appears in no doc an agent reads, and no migration note exists. The code comment claiming prune "stays a safe thing to put in a pre-commit hook" is false — a hook that prunes anything now fails the commit. | [S-03] comment asserting a safety property nothing implements | Nothing. No test or scenario asserts the documented CI recipe still works. | **FIXED** — documented in README (both spots), `docs/conceptual.ts`, and UPGRADING.md with a breaking-change note; notes recompiled. The false 'safe in a pre-commit hook' comment corrected to state the actual rule (a run that deletes nothing is never gated). | measured |
| D012 | 2026-08-17 | S1 | Adversarial review (Opus) | The consent gate broke `prune-incomplete-scan-requires-allow-incomplete.mjs`, which expects `prune --allow-incomplete` to exit 0 and delete 371 entries with no `--yes` under piped stdio. Compounding it: that scenario declares `project: 'nest-incomplete'`, so CLAUDE.md's documented gate command (`--targets local`, defaulting to `--project nest`) **never runs it**. A release-gate scenario the release-gate command does not execute is not calibration. | [S-08] a new flag's interaction with an existing guard, unexamined | The integration harness — which was never asked to run this scenario. | **FIXED** — scenario passes `--yes`; CLAUDE.md's verification block now runs both projects, and says why. | measured |
| D011 | 2026-08-17 | **S0** | Adversarial review (Opus) | A scan whose components are all ungrounded (selectors repointed at a path that no longer exists, `empty: 'until-populated'` — the policy `init` writes) still empties the baseline. Files are still WALKED, so they are observed and unclassified; no rule evaluates, every violation vanishes, every entry looks fixed. Measured: `⚠ 2 component(s) matched no files (ungrounded, provisionally green)` … `baselined debt: 1 → 0 (-1)` … `Pruned 1 fixed violation(s)`, exit 0, baseline emptied. **align printed the warning and deleted anyway.** Mechanism 3 structurally cannot see it — the files ARE observed. Pre-existing, briefly covered by the floor (D001/D002), uncovered again by its removal. | [S-10] absence treated as evidence | The floor covered exactly this and was removed for being wrong elsewhere. Nothing replaced this half. Corroborated by dead code: `buildBlindRepo()` in the new test file is defined and never called. | **FIXED** — an ungrounded component now sets the run incomplete, so ADR 023 tier 2 refuses the deletion and names the actual cause (`api → 'source/api/**'`) rather than talking about dependencies. Overridable with `--allow-incomplete`, so no trap. | measured |
| D010 | 2026-08-17 | **S0** | Adversarial review (Opus) | Mechanism 3 was added to the CLI's **delete** arm only. `store.applyMoves` still gates on mechanisms 1+2, so an orphan whose whole directory is absent is offered for a content-fingerprint match and **transferred** — forging a real human's `acceptedAt`/`acceptedBy` onto a never-reviewed violation. A transferred entry never reaches `result.removed`, so retention never sees it, and the consent gate never fires because `deletionCount` is 0. Reachable from **plain `align check`** via `persistMovedBaseline`: measured red → delete a directory → **green**, entry silently moved to an unreviewed file. Pre-existing; Stage 4 fixed one arm. | [S-09] fixed one arm, missed the other | `scan-blind-spot-retention.ts`'s own doc comment claims the containment test is shared by both consumers precisely so they cannot drift. The claim was true for mechanisms 1-2 and false the moment mechanism 3 was added to only one. | **FIXED** — mechanism 3 moved into core (`isUnderAbsentDirectory`) and called from both arms; the doc comment's claim is now true. Verified: the repro that turned red→green with a forged entry now stays red with the entry intact. **Cost**: a whole-directory rename no longer transfers (ADR 006 amended 2026-08-18, exception documented). Retired by [ADR 029](../029-2026-08-18-scan-observation-history.md) §6, which makes the refusal violation-precise rather than directory-granular. | measured |
| D009 | 2026-08-18 | S2 | `align check` on the real repo (dogfood) | The ADR reorganization moved `kluster-spike` — real TypeScript, not prose — out from under the `docs/evidence` exclude and into the scan. `align check` went provisional: 5 unresolvable specifiers across 2 files. Build, 1308 unit tests and 17 integration scenarios all stayed green. | [S-06] path-config silently widened by a file move | Nothing. No test asserts what the dogfood scan's scope is, so only running `align check` by hand caught it. | Added `docs/adr/proposals` to `align.config.ts` excludes, with the reasoning recorded beside the pattern. **No automated pin — see SHAPES.md S-06.** | measured |
| D008 | 2026-08-17 | S1 | An existing test, when the fix was applied | Mechanism 3 (the fix for D001) retains any entry whose directory produced no observed file — which includes the commonest debt-paydown path, deleting dead code. `prune-existence-probe.test.ts`'s "retention does not become a permanent leak" test failed, correctly. | [S-04] guard correct in the unsafe direction, wrong in the safe one | It did fire — an existing over-retention guard test. Recorded because the *fix* introduced it, not the original code. | ADR 006 consent gate on every deletion, turning a second command into one keystroke. Two Stage-2 fixtures re-pointed onto an observed directory, preserving their intent. | measured |
| D007 | 2026-08-17 | S3 | Adversarial review (Fable) | `integration/lib/mutations.mjs` claimed `align init` writes `empty: 'allow'`. It writes `until-populated`; `'allow'` exists only in init's throwaway in-memory probe ruleset, which is never persisted. | [S-03] comment asserting a fact about other code, unverified | Nothing — doc comments are unexecuted. | Comment corrected in place, naming both policies and where each comes from. | measured |
| D006 | 2026-08-17 | S3 | Adversarial review (Opus) | The `--forget-unscanned` test named "and only those" ended in `toEqual([])` — an assertion identical whether the hatch is scoped or forfeits the entire baseline. The scoping property was pinned only by the *next* test. | [S-05] a test that passes for the wrong reason | Nothing. The test passed, which is the problem. | Rewritten to keep a surviving entry the prefix must not reach. | measured |
| D005 | 2026-08-17 | S2 | Adversarial review (both, independently) | `describePruneOutcome` was fed the POST-split retained count, so forfeiting the only retained entry printed `Nothing to prune — no baseline entry is orphaned` directly above `Forgot 1 retained entry … is deleted`. | [S-07] a guard reintroducing the exact defect it was written to prevent | Its own unit tests — which covered 3 of the 4 count combinations and omitted the one that breaks. | Function takes the forgotten count; the destructive arm is ordered first. Fourth combination now pinned, plus `stdoutNotContains` in the integration scenario. | measured |
| D004 | 2026-08-17 | S1 | Integration harness, first run of a new scenario | `--forget-unscanned` counted forgotten entries as at-risk for ADR 023 tier 2, so on nest's `complete: false` scan the forfeiture was refused. Correct behaviour, unknown until the scenario ran. | [S-08] a new flag's interaction with an existing guard, unexamined | Nothing — the composition had never been exercised. | Not a code fix: the scenario now pins both arms, plus the contrast with a plain prune that exits 0 on the same incomplete scan. | measured |
| D003 | 2026-08-17 | S1 | Measuring a claim while writing its doc comment | The floor's literal predicate `run.observedFiles.source.size === 0` is unreachable: `align.config.ts` is a root-level `.ts` file and becomes a graph node, so the observed set is never empty. Measured `observed.source: ['align.config.ts']`. | [S-02] a predicate unreachable in practice | Nothing. It would have shipped as a guarantee that could never fire — CLAUDE.md rule 5's exact class. | Caught before shipping; predicate replaced (and later removed entirely with D001's fix). | measured |
| D002 | 2026-08-17 | S1 | Adversarial review (Opus) | The floor blocked a manifest-only/greenfield repo permanently — `prune`, `prune --allow-incomplete`, `prune --forget-unscanned` and `init` all refused, with no way out. `init` became non-idempotent. On init's seed path `dropped.length === 0`, so the refused write was provably non-destructive. | [S-01] whole-run guard against per-entry damage | Nothing. No test covered a repo with manifest rules and no source files. | Floor deleted; replaced by per-entry mechanism 3. | measured |
| D001 | 2026-08-17 | **S0** | Adversarial review (both, independently, by different routes) | ADR 028 Stage 4's floor missed a partial checkout: with one component's directory deleted and another still grounded, `align baseline prune` printed `Pruned 1 fixed violation(s)`, exited 0, and emptied the baseline. Reproduced against the built binary. The plan asserted the opposite as measured fact. | [S-01] whole-run guard against per-entry damage | Everything, and nothing: tier 1 (scan not errored), tier 2 (`complete: true`), mechanism 1 (no blind spot for an absent directory), mechanism 2 (probe correctly says absent). All four passed it. | Retention mechanism 3 (missing-directory test). Pinned by `partial-checkout-retains` (**verified red-before-green**) and `prune-retention-and-consent.test.ts`. See `D001-floor-missed-partial-checkout.md`. | measured |
| — | 2026-08-13 | S0 | Adversarial review | ADR 027 F1: an unobserved orphan misclassified as "moved", forging a real human's `acceptedAt`/`acceptedBy` onto an unreviewed violation. Ran on every plain `align check`. | [S-09] fixed one arm, missed the other | ADR 023's tiers — which guard deletion, not transfer. | `store.prune` passed `run.blindSpots` so `applyMoves` treats a blind-spot file as still known. | reconstructed |
| — | 2026-08-08 | S0 | A human reading code | BUG #18: `baseline prune` deleted every accepted entry on an errored run while printing "Pruned N fixed violation(s)" and exiting 0; `init` wrote `[]` over an existing baseline printing "Initial check is green". | [S-10] absence from a failed scan treated as evidence | Nothing. Errored gates report `violations: []`, which every consumer read as "fixed". | ADR 023's two tiers (`refuseIfRunErrored`, `refuseIfRunIncomplete`). | reconstructed |
| — | 2026-08-03 | S0 | A human reading code | BUG #10: two runs of `align init` against a config whose note block lost its END marker deleted the user's entire ruleset from `align.config.ts`. | [S-11] a writer trusted to stay inside its own region | Nothing. | ADR 026's declared write-sets, made universal and non-opt-in, plus the marker-owned-region check. | reconstructed |
| — | 2026-08-03 | S1 | A human reading code | BUG #1: a corrupted `.align/baseline.json` was read as `[]`, and the next full-snapshot write destroyed every accepted entry. | [S-10] corrupt treated as absent | Nothing. | `readBaseline` throws; every consumer catches and refuses rather than overwriting. | reconstructed |

## Reading this table

The `reconstructed` rows are deliberately thin. They are here because the *shapes* they contribute
are load-bearing — S-09, S-10 and S-11 all come from them, and S-10 has now produced three separate
severity zeros across the project's history. That recurrence is the argument for ADR 028 existing at
all, and it is only visible because the rows sit next to each other.
