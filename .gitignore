node_modules/
dist/
out/
.align/cache/
.align/telemetry.jsonl
.align/telemetry-state.json
# ADR 029: the scan-observation record is evidence about THIS checkout on THIS machine, and
# committing it would let one machine's observation authorize another machine's deletion.
.align/last-scan.json
# ADR 030's lock, gitignored 2026-08-19 (LEDGER D029). align creates and deletes it inside a single
# command, so a committed copy is always a lock somebody lost — and one of those blocks every writing
# align command on every machine that checks it out.
.align/.lock
test-apps/
*.tsbuildinfo
.DS_Store
coverage/

# npm pack artifacts (RELEASING.md inspection step)
*.tgz

# cross-version integration harness (ADR 025): run output, cloned projects, packed tarballs —
# reproducible from integration/{scenarios,projects,lib} on demand, never committed.
integration/results/

# A personal skill that materialized into this repo's skill directory. The skills that ARE committed
# here (`align/`, `bug-hunt/`) are the ones this project ships and expects a contributor to have;
# this one is the author's own toolkit and is deliberately not shared state. Targeted rather than a
# blanket `.claude/skills/` ignore, which would misdescribe the tracked ones.
.claude/skills/cli-best-practices/
